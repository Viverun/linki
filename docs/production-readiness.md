# Production readiness — C0 checkpoint and remediation register

## Scope and evidence rules

**Current phase: C1, C2-A and C2-B2 accepted by the maintainer on 2026-09-18 and merged to `main` (`origin/main` = C2-B2 fix wave + R7). C2-B1 (PR-09, PR-06) is implemented and gated on branch `c2b1-ownership` (see the C2-B1 record below); C2 is complete pending reviewer sign-off on C2-B1. C3/C4 remain unauthorized.** See the C1, C2-A, C2-B2 and C2-B1 records and the "C2 sign-off" section at the end of this document. No deployment approval is recorded.

The historical C0 verification record is preserved below: 432/432 tests, clean lint, typecheck and build on the digest-pinned Node 22 verification runtime. Its blanket dependency reachability conclusions are explicitly superseded by the C1 corrections and implementation evidence appended below. The verification runtime is not a measurement of the deployed runtime.

Conventions follow `docs/phase2-baseline.md:13` (gate tables), `docs/audit-corrections.md:88` (reproduce before fixing), and `docs/phases-0-5.md:147` (preserve corrections). Inventory came from `git ls-files`; selected tracked source sections were inspected for the register. This is a complete path inventory within the categories defined below, **not an entire line-by-line audit**, comprehensive behavioral verification, or a claim of production incidents. No secret files, databases, backup contents, or local-only artifacts are included in this inventory.

Evidence labels:

- **Static**: inspected tracked source supports the stated concern; acceptance behavior remains unverified.
- **Unverified**: carried-forward finding or runtime question without a fresh measurement here.
- **Observed**: an actual command or behavioral measurement, with environment and provenance stated. Historical observations do not establish current production behavior.

A source anchor identifies code to examine, not proof that a failure occurred in production. Existing test filenames establish coverage locations, not that those tests exercise every acceptance criterion or currently pass.

## Current checkpoint — C1 (2026-09-17), C2-A (2026-09-18), C2-B2 (2026-09-18), C2-B1 (2026-09-19)

| Field | Current status |
| --- | --- |
| Phase | C1 (490/490), C2-A (536/536) and C2-B2 (563/563) each passed the isolated Node 22.23.0 gate with lint mandatory; **all three accepted by the maintainer 2026-09-18 and merged to `main`** (see the C1 and C2 sign-off sections). C2-B2 residual R7 fixed post-merge (`951c4ec`). C2-B1 (PR-09, PR-06) passed the isolated gate (618/618 on `6966ec7`, `gate: true`, 2026-09-19) and was **accepted by the maintainer 2026-09-19 and merged to `main`** (`3b5f10e`; see the C2-B1 sign-off section). **C2 is complete.** C3 is next and remains unauthorized. |
| Scope | C1: PR-01 TLS, PR-07 navigation, PR-10 dependencies, PR-13 auth revocation/bootstrap; lint made a mandatory gate check. C2-A: PR-02 email side-effect ledger, PR-04 fail-closed migrations, PR-11 atomic run creation, PR-12 retry step identity. C2-B2: PR-03 undecided-reply hold across both tracks, open-core OOO judgment via TypeSafe/Jev with explicit threshold and fail-closed retries, operator resume route, visible LinkedIn open-core limit. |
| Gate owner | Main agent; final integrated gate recorded below (C1 image `sha256:9b3b9811…`, snapshot `84c382ba…`; C2-A image `sha256:63d201d2…`, snapshot `4a17faca…`; C2-B2 image `sha256:2bf1c2c3…`, snapshot `f1b16e4e…`) |
| Dependency residual owner | **Resolved**: maintainer approved root Nodemailer 10.0.10 with a scoped `next-auth` peer override (2026-09-17); full and production audits report 0 entries. C2-B2 added `@typesafe-ai/sdk@0.6.0`; full and `--omit=dev` audits both report 0 entries (see C2-B2 record). |
| Blockers | None. C1 `4835ffc`, C2-A `12f3953`, C2-B2 `40e99be` + R7 `951c4ec`, C2-B1 `3b5f10e` are on `main` and pushed to `origin` (Viverun/linki). |
| Next review | Before C3 authorization/deployment |
| Production status | No production changes, production mounts, or provider contact; deployed image/browser advisory assessment unverified |
| Authorized documentation change | Existing `docs/production-readiness.md` (this record); gate scripts `scripts/c0-check.mjs`, `scripts/c0-verify.mjs`; manifests `package.json`, `package-lock.json` |

## Historical C0 checkpoint fields

The following table records the earlier C0 checkpoint, not current C1 status or a new reviewer signoff. Historical claims of complete advisory triage and zero exploitability are withdrawn by the corrections below; prior test/build measurements are not rerun by this documentation update.

| Field                                                    | C0 checkpoint                                                                                                                                                                     |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Record date                                              | 2026-09-17                                                                                                                                                                        |
| Source revision                                          | `ad1bf0d3fc356b49ae261b4371554aa249140208`                                                                                                                                        |
| Authorized output                                        | `docs/production-readiness.md` only                                                                                                                                               |
| Owner / reviewer                                         | Main agent / sign-off recorded                                                                                                                                                    |
| Current phase                                            | C0 complete; ready for C1 handoff                                                                                                                                                 |
| Last completed checkpoint                                | C0 isolated baseline verification, lint debt resolution, test case reconciliation, and dependency advisory triage                                                                 |
| Current blocker                                          | None (ready for C1)                                                                                                                                                               |
| Next action                                              | Begin Phase C1 (PR-01 TLS, PR-07 CSV navigation, PR-10 dependency bump, PR-13 auth revocation)                                                                                    |
| Local CLI Node observation                               | `node --version` returned `v24.19.0`; this is not the production runtime                                                                                                          |
| Intended container family                                | Node 22, digest-pinned in `Dockerfile:6`                                                                                                                                          |
| Current deployed runtime exact version                   | **Unknown**; no container/runtime probe performed                                                                                                                                 |
| New Node 22 baseline exact version                       | `v22.23.0` (npm `10.9.8`, TS `5.9.3`, Next `16.1.6`, React `19.2.3`, better-sqlite3 `12.6.2`)                                                                                     |
| Deployment image / running revision                      | Unknown / unverified                                                                                                                                                              |
| Production health, worker count, supervisor installation | Unknown / unverified                                                                                                                                                              |
| Change / rollback scope                                  | One new Markdown file; no application rollback or data restoration needed for this documentation change                                                                           |
| Acceptance evidence / reviewer sign-off                  | **Verified complete** — 432 pass / 0 fail / 0 skip on Node 22.23.0 isolated container; lint clean (0 errors / 0 warnings); build PASS; typecheck PASS; `npm run verify:c0` exit 0 |

### Baseline ledger

| Gate                  | Prior audit, supplied with C0 request                            | New Node 22 baseline (measured 2026-09-17)                                                                                                                                          |
| --------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tests                 | Node 24: **432 passing**; historical observation, not rerun here | Node 22.23.0 isolated container: **432 pass / 0 fail / 0 skip** (after C0-F1 fix); Node 24 host clone: **432/432**                                                                  |
| Lint                  | **21 errors / 13 warnings**; historical observation              | Node 22 container: **0 errors / 0 warnings — CLEAN** (remediated from prior 21 errors / 13 warnings debt C0-L1)                                                                     |
| Build                 | Pass; historical observation                                     | **PASS** (Node 22.23.0, isolated container) after C0-F2 root-cause fix: build invoked as direct binary, not via npm wrapper                                                         |
| Typecheck             | Pass; historical observation                                     | Node 22 container: `tsc --noEmit --incremental false` **exit 0** (TS 5.9.3)                                                                                                         |
| Dependency advisories | Fresh advisory assessment unverified                             | **Triage complete** (measured 2026-09-17): 20 vulnerable package entries (2 critical, 13 high, 5 moderate); the prior zero-exploitability conclusion is withdrawn; see C1 corrections and residual |

Do not silently substitute earlier documentation totals for the supplied 432-test baseline. `docs/phases-0-5.md:19` records different historical totals and `docs/phase2-baseline.md:162` explains reconciliation requirements. Test **files** below are not runtime test **cases**. Reconcile tracked selection and runtime-expanded cases without enumerating or reading local-only artifacts.

Commands are defined in `package.json:5`: lint via `npm run lint`, tests via `npm test`, build via `npm run build`; historical typechecking uses `npx tsc --noEmit` (`docs/phase2-baseline.md:19`). Run these only in the main agent's isolated tracked-only Node 22 environment. `prebuild` runs the mirror script (`package.json:8`), and default test globs are not themselves a tracked-only boundary. This documentation pass did not run those gates or create build/typecheck artifacts. First address test environment isolation in PR-16; do not inherit live backup destinations or webhook configuration.

For each measured checkpoint append: timestamp; owner; source revision and intended diff; exact Node/npm/compiler versions; image identity if applicable; isolation method; command and exit code; pass/fail/skip or lint counts; sanitized evidence reference; findings confirmed/retracted; rollback readiness; unresolved blockers; next action; reviewer decision. Never include secret values or production row contents.

## Tracked inventory

Observed from `git ls-files`: **214 tracked files**, before this new untracked document. Initial `git diff --name-only` was empty for tracked files. No untracked-file enumeration was used for this inventory.

| Category                                       | Count | Definition                                                                                              |
| ---------------------------------------------- | ----: | ------------------------------------------------------------------------------------------------------- |
| API route files                                |    71 | Every tracked file under `pages/api/`; files, not HTTP method count                                     |
| UI page files                                  |    15 | Every tracked non-API file under `pages/`                                                               |
| Mutation-capable UI pages                      |    11 | Explicit mutation/authentication call sites; includes dashboard refresh POST and login                  |
| Read-only UI pages                             |     2 | Company detail and email health                                                                         |
| Page scaffolding                               |     2 | `_app.tsx`, `_document.tsx`                                                                             |
| Job/bootstrap modules                          |     8 | Listed below; includes runner-invoked maintenance, not eight independent workers                        |
| Integration implementation/support modules     |    18 | Listed below; includes browser helpers and type/TLS support, not eighteen providers                     |
| Other shared library modules                   |    15 | Includes two colocated test suites; together with integrations accounts for all 33 tracked `lib/` files |
| Operational/development/test scripts           |    14 | Every tracked file under `scripts/`, including hook and test harnesses                                  |
| Test suites                                    |    46 | 44 under `tests/`, 2 under `lib/linkedin/`, all ending `.test.ts`                                       |
| Test support under `tests/`                    |     1 | `tests/support/source-text.ts`; script harnesses counted separately                                     |
| Deployment/build/lint/TypeScript configuration |     8 | Explicit supporting files listed below                                                                  |

Categories overlap: job modules include integration/shared modules; colocated suites also appear in the test inventory; API routes can initiate jobs. Counts must not be summed as disjoint subsets of 214.

### API routes — complete grouped inventory (71)

Paths below are relative to `pages/api/`. Braces enumerate literal alternative filenames, not assumed routes.

| Group          | Count | Paths                                                                                                                                                                                     |
| -------------- | ----: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Accounts       |     6 | `accounts/index.ts`; `accounts/[id]/{authenticate,index,li-stats,login,sync-accepted}.ts`                                                                                                 |
| Activity logs  |     1 | `activity-logs/index.ts`                                                                                                                                                                  |
| Authentication |     3 | `auth/{[...nextauth],change-password,signup}.ts`                                                                                                                                          |
| Companies      |     2 | `companies/{[id],index}.ts`                                                                                                                                                               |
| Dashboard      |     2 | `dashboard/{agent-stats,stats}.ts`                                                                                                                                                        |
| Email accounts |     4 | `email-accounts/index.ts`; `email-accounts/[id]/{index,send-test,test}.ts`                                                                                                                |
| Imports        |     2 | `imports/index.ts`; `imports/[id]/cancel.ts`                                                                                                                                              |
| Inbox          |     4 | `inbox/{index,reply,thread}.ts`; `inbox/[replyId]/cancel-followup.ts`                                                                                                                     |
| Integrations   |     1 | `integrations/index.ts`                                                                                                                                                                   |
| Lists          |    15 | `lists/index.ts`; `lists/[id]/{add-members,analyze,apollo-enrich,conflicts,csv-template,enrich,import-csv,import-status,import,index,move-targets,remove-members,sync-status,targets}.ts` |
| Runs           |     7 | `runs/index.ts`; `runs/[id]/{enroll,index,remove,retry,start,unenroll}.ts`                                                                                                                |
| Settings       |     1 | `settings/import-cap.ts`                                                                                                                                                                  |
| System         |     1 | `system/update.ts`                                                                                                                                                                        |
| Targets        |     5 | `targets/{[id],index}.ts`; `targets/[id]/{emails,profile-scrape,runs}.ts`                                                                                                                 |
| Templates      |     2 | `templates/{[id],index}.ts`                                                                                                                                                               |
| Todos          |     2 | `todos/{[id],index}.ts`                                                                                                                                                                   |
| Workflows      |     9 | `workflows/index.ts`; `workflows/[id]/{analytics,duplicate,enrollments,index,prospects,stats,steps}.ts`; `workflows/[id]/steps/[stepId].ts`                                               |
| Standalone     |     4 | `email-health.ts`; `health.ts`; `premium-status.ts`; `tour.ts`                                                                                                                            |

No enterprise-only route is counted merely because a UI string references it. Open-core/premium boundaries are described in `docs/phases-0-5.md:131` and implemented through `lib/premium.ts`.

### UI pages and shared mutation surfaces

All paths below are relative to `pages/`:

- Mutation-capable (11): `companies/index.tsx`; `contacts/[id].tsx`; `contacts/index.tsx`; `inbox.tsx`; `index.tsx`; `lists/[id].tsx`; `lists/index.tsx`; `login.tsx`; `settings.tsx`; `workflows/[id].tsx`; `workflows/index.tsx`.
- Read-only page implementations (2): `companies/[id].tsx`; `email-health.tsx`.
- Scaffolding (2): `_app.tsx`; `_document.tsx`.

All six tracked components, including shared session/tour/health UI surfaces: `components/layout/{Layout,RunnerHealthBanner,Sidebar}.tsx`; `components/onboarding/TourGate.tsx`; `components/ui/{FilterBar,ModelPicker}.tsx`. Shared tour mutation logic is in `lib/tour.ts`; page classification does not exclude mutations from shared components.

### Jobs, integrations, and supporting libraries

Job/bootstrap modules (8):

- `instrumentation.ts`: startup registration for runner and watchdog (`:5`).
- `lib/linkedin/runner.ts`: track execution, claims, periodic maintenance, inbox/import orchestration, alerts, watchdog.
- `lib/import-jobs.ts`: scheduled import batches, cancellation, quota and continuation handling (`:120`).
- `lib/update-check.ts`: update polling scheduler (`:83`).
- `lib/email/inbox.ts`: runner-invoked email reply/bounce synchronization (`:157`).
- `lib/linkedin/enrich.ts`: list enrichment work.
- `lib/linkedin/li-stats.ts`: account statistics refresh work.
- `lib/linkedin/sync-accepted.ts`: accepted-connection synchronization work.

Request-triggered job entrypoints are included in the complete route table: account authenticate/login/li-stats/sync-accepted; list import/enrich/apollo-enrich/sync-status; target profile-scrape; run start; email test/send-test/inbox reply; system update. Helper and request-bound operations are not asserted to be durable jobs. There is no tracked standalone `jobs/` or `worker/` tree; operational scheduling also exists in the script inventory.

Integration implementation/support modules (18), complete relative to `lib/`:

- Apollo and premium boundary (2): `apollo.ts`; `premium.ts`.
- SMTP/IMAP (4): `email/{imap-simple.d.ts,inbox.ts,sender.ts,tls.ts}`.
- LinkedIn/browser (12): `linkedin/{connect,enrich,li-stats,message,pending-invitations,profile-scrape,resolve-account,runner,scraper,session,sync-accepted,visit}.ts`.

Remaining tracked library files (15): `api-validate.ts`; `auth.ts`; `crypto.ts`; `csv-import.ts`; `db.ts`; `health-contract.ts`; `import-jobs.ts`; `linkedin-url.ts`; `page-auth.ts`; `rate-limit.ts`; `tour-pages.ts`; `tour.ts`; `update-check.ts`; `linkedin/connect.test.ts`; `linkedin/session.test.ts`. Together with the 18 integration modules, these account for all **33** tracked `lib/` files.

Additional integration boundaries: authentication in `lib/auth.ts`, `lib/page-auth.ts`, `proxy.ts`, and the auth routes; update/network operations in `lib/update-check.ts` and the system route; alert webhook in `lib/linkedin/runner.ts:354`; backups and mirror/enterprise build hooks in the script inventory. No provider was contacted and no credentials were inspected.

### Scripts and deployment support — complete inventory

Every tracked `scripts/` path (14):

| Purpose                                   | Paths relative to `scripts/`                              |
| ----------------------------------------- | --------------------------------------------------------- |
| Backup/recovery                           | `backup.mjs`; `restore-drill.ts`                          |
| Health/deploy supervision                 | `health-predicate.js`; `watchdog.sh`; `verify-deploy.mjs` |
| Operator repair / live-capable diagnostic | `force-next-step.js`; `retest-connect.ts`                 |
| Enterprise build mirroring                | `mirror-ee.js`; `strip-mirrored-pages.js`                 |
| Quality gates / mutation harness          | `preflight.sh`; `mutate.sh`; `hooks/pre-commit`           |
| Test bootstrap/resolution                 | `test-setup.mjs`; `test-resolve-hooks.mjs`                |

Deployment/build/lint/TypeScript support (8): `Dockerfile`; `docker-compose.yml`; `.dockerignore`; `next.config.ts`; `postcss.config.mjs`; `eslint.config.mjs`; `tsconfig.json`; `package.json`. Dependency resolution: `package-lock.json`. Bootstrap/auth entrypoints: `instrumentation.ts`; `proxy.ts`. These paths are inventoried, not executed.

### Test suites — complete inventory (46 + 1 support file)

Colocated suites (2): `lib/linkedin/connect.test.ts`; `lib/linkedin/session.test.ts`.

Under `tests/` (44 suites):

```text
accounts-api.test.ts                 backup.test.ts
 db-busy-timeout.test.ts             db-init-atomicity.test.ts
 degraded-alerting.test.ts           enroll-scheduling.test.ts
 first-degree-detection.test.ts      health-endpoint.test.ts
 health-isolation.test.ts            health-predicate.test.ts
 host-allowlist.test.ts              host-expression-drift.test.ts
 inmail-idempotency.test.ts          message-authorization.test.ts
 message-idempotency.test.ts         null-vanity.test.ts
 partial-update.test.ts              phase2-validation.test.ts
 phase3-guards.test.ts               phase4-guards.test.ts
 phase5-guards.test.ts               proxy-health-bypass.test.ts
 retry-ledger.test.ts                run-lifecycle-guards.test.ts
 run-status-transition.test.ts       runner-claim.test.ts
 runner-connect-idempotence.test.ts  runner-loop-survival.test.ts
 runner-visit-degree.test.ts         runner-watchdog.test.ts
 secret-migration.test.ts            session-read-path.test.ts
 signup-closed.test.ts               slice-c-connection-state.test.ts
 source-text-corpus.test.ts          source-text-drift.test.ts
 ssr-auth.test.ts                    step-identity.test.ts
 support/source-text.test.ts         todos-activity-logs.test.ts
 visit-degree-observation.test.ts   visit-topcard.test.ts
 workflow-delete-guard.test.ts       workflow-duplicate.test.ts
```

Non-suite support: `tests/support/source-text.ts`. The three test-related scripts (`retest-connect.ts`, `test-setup.mjs`, `test-resolve-hooks.mjs`) are already counted among 14 scripts. API `test.ts`/`send-test.ts` routes are not test suites.

## Remediation phases

These are proposed C-series work allocations, not the historical Phases 0–5 and not authorization to implement them:

- **C0**: inventory, isolated Node 22 baseline, evidence and acceptance planning.
- **C1**: transport/authentication boundaries and dependency assessment.
- **C2**: durable state, side-effect identity, import and worker ownership.
- **C3**: lifecycle and truthful UI behavior.
- **C4**: operational recovery, alerting, test isolation and quality gates.

Implementation requires separate authorization. Each row remains open until its acceptance evidence and reviewer decision are appended. Tests described below use synthetic fixtures, mocked provider/browser boundaries and isolated temporary storage; they are behavior specifications, not production reproduction instructions.

## Remediation register

Finding descriptions and source anchors below preserve the C0 baseline. Current C1 implementation and focused evidence are appended below; PR-01, PR-07 and PR-13 are implemented pending final integrated gate/reviewer, not still unimplemented. No acceptance row is closed merely by these focused results.

| ID    | Phase   | Finding and evidence (`file:line`)                                                                                                                                                                                                                                                                                                                                                                                           | Behavioral acceptance test                                                                                                                                                                                                                                                                                                                                           | Evidence status / disposition                                                                                                                                                   |
| ----- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PR-01 | C1      | **TLS verification**: `lib/email/tls.ts:11` disables certificate verification centrally; SMTP uses it at `lib/email/sender.ts:32`. Centralization is not strict verification.                                                                                                                                                                                                                                                | In an isolated mail fixture, trusted certificates work; untrusted, expired and hostname-mismatched certificates fail by default. Any compatibility exception is explicit, scoped and visible, and both SMTP and IMAP obey the policy.                                                                                                                                | **Observed (C1 gate 2026-09-17)**: strict trust/expiry/host-identity verification for SMTP and IMAP, SMTP requires TLS; 31 loopback fixture tests pass inside the final integrated gate. Provider interoperability unverified; C1 accepted 2026-09-18. |
| PR-02 | C2      | **SMTP side-effect ledger**: send precedes context/advance persistence at `lib/linkedin/runner.ts:1587`; `lib/email/sender.ts:39` delegates send to the transport; retry excludes email from the ledger path at `pages/api/runs/[id]/retry.ts:130`.                                                                                                                                                                          | Simulate successful transport acceptance followed by bookkeeping failure/restart. Ordinary retry sends nothing twice; ambiguous delivery is held for explicit resolution. Preserve stable sender/recipient/step identity and distinguish transport acceptance from delivery confirmation, including manual inbox replies.                                            | **Observed (C2-A gate 2026-09-18)**: email steps record intent in `step_side_effects` before send; SMTP failures classified pre_send/rejected (abandoned) vs ambiguous (held in_flight); retry blocks, `mark_delivered`/`resend` resolve; 11 tests in `tests/email-idempotency.test.ts`, 20 in `tests/smtp-failure-classification.test.ts`. Transport acceptance only — bounce scan remains the delivery-failure signal. Accepted 2026-09-18 (C2 sign-off). |
| PR-03 | C2      | **Open-core reply suppression**: reply capture does not eagerly stamp suppression and dispatch is premium-dependent (`lib/email/inbox.ts:232`); runner suppression reads reply timestamps (`lib/linkedin/runner.ts:1013`); LinkedIn reply detection is premium-gated (`:1833`).                                                                                                                                              | With premium absent, a captured human email reply prevents subsequent automated follow-ups before the next send; capture/classifier failures cannot silently authorize another send. Define and visibly enforce open-core LinkedIn capability limits and explicit OOO policy.                                                                                        | **Observed (C2-B2 gate 2026-09-18)**: undecided email replies hold both tracks before any send (`tests/reply-hold.test.ts`); open-core policy judges out-of-office via TypeSafe/Jev with an explicit threshold (default 0.9), fails closed after 3 judgment failures, and records decisions in the reply row (`tests/reply-policy.test.ts`); operator resume route; LinkedIn open-core limit stated by `/api/premium-status` and the UI. Judgment quality on real mail **Unverified** (synthetic samples only); LinkedIn reply detection remains premium. Accepted 2026-09-18 (C2 sign-off). |
| PR-04 | C2      | **Migrations**: migration loop broadly catches execution errors (`lib/db.ts:631`); existing initialization protection is documented at `lib/db.ts:23`.                                                                                                                                                                                                                                                                       | Across fresh and supported old synthetic schemas, migrations converge. Inject a non-duplicate DDL failure: startup fails visibly, does not publish a usable half-initialized connection, and retry safely resumes. Duplicate-column cases alone are treated as already applied.                                                                                      | **Observed (C2-A gate 2026-09-18)**: `migrations[]` loop tolerates only duplicate-column/already-exists errors, runs in one IMMEDIATE transaction; injected missing-column and write-lock faults fail `getDb()` loudly with no cached handle and resume once fixed (`tests/migrations-fail-closed.test.ts`). Residual: the presence-guarded legacy rebuild functions below the loop still swallow errors (they toggle `PRAGMA foreign_keys`, which a transaction would neutralise) — open, C4 candidate. Accepted 2026-09-18 (C2 sign-off). |
| PR-05 | C4      | **Watchdog shell/control flow**: `set -euo pipefail` (`scripts/watchdog.sh:14`), history-dependent write lists (`:42`, `:60`), restart/budget path (`:49`).                                                                                                                                                                                                                                                                  | With a fake health predicate and fake restart command, empty initial history reaches the configured threshold without premature exit; successful restart persists budget; failures remain recoverable. Healthy and non-restartable states do not restart; concurrent invocations cannot lose budget updates.                                                         | **Static** concern; actual shell exit behavior and installed supervisor **unverified**.                                                                                         |
| PR-06 | C2      | **Import recovery / false checkpoints**: only scheduled rows are selected and ownership is process-local (`lib/import-jobs.ts:117`); completion and continuation are separate writes (`:193`); missing page responses can still advance `lastPage` (`lib/linkedin/scraper.ts:331`, `:416`).                                                                                                                                  | Restart an interrupted synthetic import and recover or explicitly quarantine it. Missing page data must not count as verified progress/exhaustion. Profile writes, durable checkpoint and continuation remain consistent under injected failure; cancellation preserves truthful state and quota.                                                                    | **Observed (C2-B1 gate 2026-09-19)**: atomic claim with a per-claim owner token; per-page durable inserts + checkpoint under an owner fence; a missing page never advances the checkpoint; stall/cancel truthful; a stale running row is recovered from page+1 or quarantined after 3 stalls, and recovery finishes an exhausted window as `done` and honours `cancel_requested` (`tests/import-claim.test.ts`, `tests/import-checkpoints.test.ts`, `tests/scraper-checkpoints.test.ts`). `importedToday` counts running rows regardless of date plus terminal rows by `finished_at` (ruling R12). Synthetic imports only — not live Sales Navigator. **Closed at its register boundary — maintainer sign-off 2026-09-19** (see the C2-B1 sign-off section). |
| PR-07 | C1      | **CSV navigation boundary**: `sales_nav_url` is accepted separately at `lib/csv-import.ts:97`, persisted at `:146`, and consumed by browser navigation at `lib/linkedin/enrich.ts:67`.                                                                                                                                                                                                                                       | Synthetic CSV imports reject unsupported destination schemes/hosts/shapes before persistence or navigation; navigation boundaries independently enforce the approved destination policy. Valid supported Sales Navigator links still enrich; rejected rows produce honest row errors and no browser action.                                                          | **Observed (C1 gate 2026-09-17)**: 17 focused tests pass inside the final integrated gate. Route interception is not full network confinement (redirect hops, service workers); no live provider test; C1 accepted 2026-09-18. |
| PR-08 | C3      | **Deletion lifecycle**: target deletion removes run profiles/logs (`pages/api/targets/[id].ts:50`; bulk `pages/api/targets/index.ts:153`); account deletion performs a database delete (`pages/api/accounts/[id]/index.ts:40`); session lifecycle exists at `lib/linkedin/session.ts:331`.                                                                                                                                   | For active and idle target/account/list/workflow/run fixtures, deletion is either refused consistently or coordinated with cancellation and owned-resource teardown. No late external action or orphaned browser remains after successful deletion; foreign keys and accounting/audit retention policy hold.                                                         | **Static**; open. A target is not a browser-owning account: do not blindly add `closeSession(targetId)`. Earlier cascade-gap claims were corrected in `docs/phases-0-5.md:135`. |
| PR-09 | C2      | **Worker claims / browser ownership**: atomic claim anchor `lib/linkedin/runner.ts:671`; page queue releases via timeout (`lib/linkedin/session.ts:155`, `:172`); scraper directly opens context pages (`lib/linkedin/scraper.ts:367`).                                                                                                                                                                                      | Two isolated worker instances cannot concurrently execute one logical action. A stale owner cannot write after ownership transfer. A slow browser operation and an import obey the same ownership rules; timeout either ends owned work or prevents overlapping replacement. Verify and enforce any single-process deployment precondition.                          | **Observed (C2-B1 gate 2026-09-19)**: DB runner lease (`app_settings.runner_lease`, TTL 600 s, renewed on every fenced track write and before each step) enforces the single-process precondition, with a second process reporting standby at `/api/health` (`tests/runner-lease.test.ts`, `tests/health-lease.test.ts`); per-account browser owner (`lib/linkedin/ownership.ts`) claims its slot synchronously before any `await`, with an abortable max hold and no overlapping replacement; every context bypass migrated; a busy browser reschedules the step instead of overlapping it (`tests/browser-ownership.test.ts`, `tests/runner-step-busy-browser.test.ts`). Verified with fake browser objects and one process — not with two real Chromium stacks. **Closed at its register boundary — maintainer sign-off 2026-09-19** (see the C2-B1 sign-off section). |
| PR-10 | C1      | **Dependency advisories**: dependency declarations `package.json:16`, pinned Next version `:28`, image/browser pins `Dockerfile:6`, `:25`; resolution artifact `package-lock.json:1`.                                                                                                                                                                                                                                        | Produce a dated dependency/image advisory report with exact installed/resolved versions, affected ranges and reachable production/dev distinctions. Candidate upgrades pass isolated build/auth/mail/browser smoke gates; unresolved advisories have an explicit owner, rationale and review deadline.                                                               | **Observed (C1, 2026-09-17), residual resolved**: root Nodemailer upgraded to 10.0.10 with a scoped `next-auth` peer override approved by the maintainer; full and production `npm audit` each report **0** entries; final integrated gate passed on the new lockfile. npm audit covers the npm graph only; image/browser assessment unverified; C1 accepted 2026-09-18. |
| PR-11 | C2      | **Run-create partial writes**: parent insert precedes explicit target validation (`pages/api/runs/index.ts:69`, `:81`); child transaction starts later (`:174`).                                                                                                                                                                                                                                                             | Invalid target/sender selections and injected child-write failures leave zero new parent/child/track rows. Successful creation is atomic; concurrent requests obey the active-workflow contract. Do not accept an HTTP 400 alone as evidence of rollback.                                                                                                            | **Observed (C2-A gate 2026-09-18)**: validate → compute → single IMMEDIATE transaction; injected child-insert failure leaves zero run/profile/track rows; active-run 409 decided inside the write lock (`tests/run-create-atomic.test.ts`). Accepted 2026-09-18 (C2 sign-off). |
| PR-12 | C2      | **Retry identity**: retry falls back from pinned ID to index (`pages/api/runs/[id]/retry.ts:128`) and advances using stored index (`:181`, `:209`).                                                                                                                                                                                                                                                                          | Reorder or remove a pinned step in an isolated failed-track fixture: retry resolves by stable identity or blocks explicitly, never picks an unrelated replacement. Advancement uses the resolved step position; legacy ledger rows still prevent duplicate actions; operator assertions remain distinguishable.                                                      | **Observed (C2-A gate 2026-09-18)**: retry uses the runner's `resolveStep`; reordered → by id, deleted → blocked, legacy null-id → by index and pinned on re-arm; advance uses the resolved index (`tests/retry-step-identity.test.ts`). Accepted 2026-09-18 (C2 sign-off). |
| PR-13 | C1      | **Revocation / bootstrap**: token presence authenticates at `lib/auth.ts:22`; password change updates only the hash (`pages/api/auth/change-password.ts:36`); owner count precedes asynchronous hashing/insert (`pages/api/auth/signup.ts:31`, `:66`).                                                                                                                                                                       | Password change/revocation invalidates previously issued sessions under the chosen policy for API and SSR. Concurrent first-owner requests produce one owner only. Closed signup remains closed, and a fresh authenticated owner can still sign in.                                                                                                                  | **Observed (C1 gate 2026-09-17)**: session-version revocation and atomic first-owner signup covered by the final integrated gate; built-server HTTP smoke recorded in the C1 evidence section. Internal service secret rotation out of scope; C1 accepted 2026-09-18. |
| PR-14 | C3      | **Bounce scope**: candidates are extracted broadly (`lib/email/inbox.ts:295`); skips are not filtered to the email track (`:313`); company siblings are also invalidated (`:318`).                                                                                                                                                                                                                                           | Synthetic recipient-specific delivery failures affect only the correlated recipient and appropriate email work. Unrelated recipients, LinkedIn tracks, company siblings and temporary failures remain unaffected unless explicit stronger evidence/policy warrants broader suppression. Reprocessing is idempotent.                                                  | **Static**; open. No production misclassification asserted.                                                                                                                     |
| PR-15 | C3      | **UI false-success**: mutation responses are discarded at `pages/companies/index.tsx:98`, `pages/settings.tsx:878`, `pages/lists/index.tsx:96`, `pages/workflows/index.tsx:223`. These are examples, not a full handler audit.                                                                                                                                                                                               | For every mutation-capable page and shared mutation surface, exercise success, 4xx, 5xx, network failure and non-JSON responses. Failure leaves truthful UI state and visible errors, never a success toast, removed row or completed navigation; retry/reconciliation handles ambiguous outcomes.                                                                   | **Static** sampled call sites; complete behavior sweep **unverified**.                                                                                                          |
| PR-16 | C0 / C4 | **Test environment backup/webhook isolation**: bootstrap only installs resolver (`scripts/test-setup.mjs:1`); backup destination may fall back to environment (`scripts/backup.mjs:184`), including tests supplying no offsite option (`tests/backup.test.ts:40`); early alert tests invoke outcome notification (`tests/degraded-alerting.test.ts:54`) and runner reads webhook environment (`lib/linkedin/runner.ts:354`). | Before importing application modules, isolate all data/backup paths and remove or replace external webhook settings. With harmless sentinel inherited configuration, tests write only temporary fixture files and contact only mocked/loopback receivers. All environment overrides are restored, and full-suite tracked selection is documented.                    | **Observed**: verified in isolated Node 22 verification container (`--network=none`, loopback only, clean environment); gate execution isolated.                                |
| PR-17 | C4      | **Secondary backups**: primary verifies before promotion (`scripts/backup.mjs:217`), but secondary uses direct copy then prune (`:240`); same-volume limitations are documented at `docs/backup-restore.md:115`.                                                                                                                                                                                                             | A failed/interrupted secondary copy never looks like a completed valid backup and never prunes the last valid secondary. Verify secondary integrity before promotion/retention, detect capacity/collision failures, and restore with separately escrowed synthetic encryption material. Establish actual independent failure domain and measured RPO/RTO separately. | **Static**; actual backup locations, restore readiness and secondary durability **unverified**.                                                                                 |
| PR-18 | C4      | **Alert dismissal**: session-stored dismissal (`components/layout/RunnerHealthBanner.tsx:31`) hides matching keys (`:100`) and is set on dismissal (`:103`).                                                                                                                                                                                                                                                                 | Dismiss an outage, observe recovery, then trigger a new outage of the same class in the same tab: the new incident becomes visible. Escalation and network failure remain visible; storage-disabled behavior and session lifetime are explicit.                                                                                                                      | **Static**; open. No missed production alert claimed.                                                                                                                           |
| PR-19 | C4      | **Lint debt**: command `package.json:11`; prior residual 21/13 recorded in `docs/phases-0-5.md:144` and supplied C0 baseline.                                                                                                                                                                                                                                                                                                | Capture exact tracked-only Node 22 findings, remediate in separately authorized review-sized changes, and reach zero errors with warnings either resolved or explicitly owned. Hooks-sensitive UI changes preserve behavior under component tests; do not merely disable rules or broaden ignores.                                                                   | **RESOLVED**: **0 errors / 0 warnings** on Node 22 verification container (`eslint --max-warnings=0` clean; C0-L1 closed).                                                      |

## Exit criteria and handoff

**Historical C0 exit declaration (advisory conclusion corrected below; not C1 acceptance):**

1. **Baseline and gate repeatability**: Verified on Node v22.23.0 in digest-pinned isolated Docker container via `npm run verify:c0` (exit 0). Isolation probes pass, typecheck passes, 432/432 tests pass, build passes.
2. **Lint debt resolved**: Remediated from 21 errors / 13 warnings to **0 errors / 0 warnings** (`eslint --max-warnings=0` clean).
3. **Inventory and test reconciliation complete**: 46 tracked test suites reconciled to exactly 432 runtime test cases (table below).
4. **Dependency advisory correction**: C0 reported 20 vulnerable package entries, not 20 distinct advisories. Its claim of complete reachability triage and zero production exploitability is withdrawn. C1 upgrades and the remaining PR-10 residual are recorded below; image/browser assessment remains unverified.
5. **Reviewer sign-off**: Recorded. Phase C0 is closed; handoff to Phase C1 authorized.

## C0 measurement record (appended 2026-09-17)

Owner: main agent. Reviewer: sign-off recorded. Environment: disposable Docker containers from digest-pinned `node:22-slim` (sha256:d9f8500…b0b2, resolves to Node **v22.23.0**), `--network=none`, non-root uid 1000, `--cap-drop=ALL`, no-new-privileges, read-only rootfs, exec-tmpfs `/work`, no production mounts/credentials; evidence dirs `/tmp/linki-c0-*` with manifest (snapshot sha256 per run), `image-id.txt`, `prepare.log`, `checks.log`.

### Findings closed during C0

| ID                 | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Evidence                                                                                                           | Disposition                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C0-F1** (closed) | `mock.module({ exports })` is **silently ignored on Node 22** — mocks never apply, mocked behaviors test the real module. Direct probe: mocked `randomUUID` still returned the original value on 22; 11 mock-heavy suites would have been vacuous on the deployment runtime while green on 24.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Probe outputs recorded in session; `mock.module.bind` casts across 13 test files; `tests/signup-closed.test.ts:42` | Fixed: converted all 13 files to `namedExports` (works on both runtimes; deprecated-only on 24, warnings disabled); `signup.ts` switched to `import { hash } from "bcryptjs"` because Node 22's mock shim cannot emit a `default` binding (`export let default` is a SyntaxError). Verified: 432/432 on Node 24 host clone **and** Node 22 container.       |
| **C0-F2** (closed) | **`next build` fails when spawned through `npm run` on Node 22 in the verification container.** In the gate harness, `npm run build` failed 8/8 with `TypeError: Cannot read properties of null (reading 'useState')` during prerender (failing page varied: `/inbox`, `/`, `/login`, `/email-health`; all frames suppressed by Next's ignore-listing). Same workspace, seconds apart: direct `node node_modules/next/dist/bin/next build` passed and npm-wrapped build failed (A/B npm=1 direct=0, reproduced twice; also 15/15 passes via sh-parent). Ruled out by controlled A/B: workspace leftovers (wipe), `@/ee` require (neutralized — `EE-NOT-THE-CAUSE`), parent process type (host node-parent passes), CPU/memory caps, lint-before-build, Turbopack-vs-webpack (both pass direct), deterministic app defect (page varies; `_app` hook guards verified). Root cause: the npm wrapper layer (Node 22 + npm 10.9.8 + container). | A/B runs in image 2fd37a486d68, session logs `/tmp/opencode/c0-*.log`, WRAP run                                    | **Fixed in harness** (`scripts/c0-check.mjs`): build check now runs `prebuild` (mirror-ee) explicitly, then `next build` as a direct binary — identical to what the npm script would do, minus the npm layer. **Final gate run: `"gate":true` — all checks green on Node 22.23.0** (typecheck, 432/432 tests, build; lint = recorded C0-L1 debt by design). |
| **C0-F3** (closed) | Harness container usability: noexec `/tmp` blocked native `.node` mapping ("failed to map segment"); Turbopack rejected out-of-root `node_modules` symlink; git "dubious ownership" for uid 1000 on root-copied files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `scripts/c0-check.mjs` exec tmpfs `/work`; real node_modules copy; `git config safe.directory /work`               | Fixed in harness; verified by subsequent green isolation/loopback/egress probes and 432/432 in-container suite.                                                                                                                                                                                                                                             |
| **C0-L1** (closed) | Residual lint debt (21 errors / 13 warnings) from historical phases resolved to **0 errors / 0 warnings**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `npm run lint -- --max-warnings=0` in Node 22 container exited 0; 0 errors, 0 warnings.                            | Fixed: hooks dependency arrays, unused variables/functions, explicit types, and external store subscriptions cleanly updated across 10 files. Verified clean under `--max-warnings=0`.                                                                                                                                                                      |

### Repro matrix (all in verification image 07490b710f60 / 2fd37a486d68 unless noted)

| #   | Parent                          | Steps before build                                               | `--read-only` | Result                                                              |
| --- | ------------------------------- | ---------------------------------------------------------------- | ------------- | ------------------------------------------------------------------- |
| 1–4 | sh                              | typegen / typegen+typecheck / typegen+tests / typegen+tests+lint | yes           | **PASS ×4**                                                         |
| 5–6 | sh                              | typegen+tests+lint ×2                                            | yes           | **PASS ×2**                                                         |
| 7   | sh                              | 4000 fake log lines + stdio:inherit                              | yes           | **PASS ×3**                                                         |
| 8   | sh                              | full gate sequence                                               | yes           | **PASS**                                                            |
| 9   | node (host, project clone)      | full sequence                                                    | n/a           | **PASS ×2**                                                         |
| 10  | node (in container, gate steps) | full gate sequence                                               | yes           | **FAIL ×6+** (gate runs 7,8,9,10 + CELL/CELL2/X)                    |
| 11  | node (container)                | typegen+tests only                                               | yes           | **FAIL** (isolates: tests-before-build + node parent is sufficient) |
| 12  | node (container)                | full gate + `@/ee` neutralized                                   | yes           | **FAIL** (`EE-NOT-THE-CAUSE`)                                       |
| 13  | node (container)                | full gate + artifact wipe + rebuild                              | yes           | **FAIL** (leftovers ruled out)                                      |
| 14  | node (host)                     | full gate sequence                                               | n/a           | **PASS ×2** — container-specific                                    |

Conclusion: the failure requires (a) the containerized environment, (b) a Node (not sh) parent process, and (c) the full test suite having run in that same parent chain first. It is a **harness/runtime interaction**, not demonstrated application code failure; the identical source builds clean via `Dockerfile` (`docker build` path) and in every sh-parent context.

### Gate verdict (honest)

- `verify:c0` exit **0** on final run: **all checks green on Node 22.23.0** — isolation probes (non-root, loopback up, external egress unreachable, clean env), typecheck, tests 432/432/0, lint (0 errors / 0 warnings), prebuild, build.
- Historical C0 record: 46-suite test inventory reconciled to 432 test cases and reviewer acceptance recorded at that checkpoint. The broader claim that all scope gaps, including PR-10 reachability, were closed is superseded by the C1 corrections below; it is not current dependency or C1 acceptance.

### Test suite inventory and case reconciliation (46 suites / 432 tests)

Measured on Node 22.23.0 via `--test-reporter=tap`:

| #         | Suite                                      |   Passing tests | Notes / parameterization                                       |
| --------- | ------------------------------------------ | --------------: | -------------------------------------------------------------- |
| 1         | `lib/linkedin/connect.test.ts`             |              15 | Colocated suite                                                |
| 2         | `lib/linkedin/session.test.ts`             |              20 | Colocated suite                                                |
| 3         | `tests/accounts-api.test.ts`               |               5 | API account endpoints                                          |
| 4         | `tests/backup.test.ts`                     |              16 | Backup creation and rotation                                   |
| 5         | `tests/db-busy-timeout.test.ts`            |               7 | SQLite busy_timeout enforcement                                |
| 6         | `tests/db-init-atomicity.test.ts`          |               3 | DB initialization locks                                        |
| 7         | `tests/degraded-alerting.test.ts`          |              17 | Health banner alert states                                     |
| 8         | `tests/enroll-scheduling.test.ts`          |               9 | Campaign enrollment timing                                     |
| 9         | `tests/first-degree-detection.test.ts`     |              11 | 1st-degree connection checks                                   |
| 10        | `tests/health-endpoint.test.ts`            |              16 | `/api/health` status codes                                     |
| 11        | `tests/health-isolation.test.ts`           |               4 | Health endpoint independence                                   |
| 12        | `tests/health-predicate.test.ts`           |              12 | Supervisor health predicate                                    |
| 13        | `tests/host-allowlist.test.ts`             |               8 | Allowed navigation hosts                                       |
| 14        | `tests/host-expression-drift.test.ts`      |               3 | Host regex tripwires                                           |
| 15        | `tests/inmail-idempotency.test.ts`         |               5 | InMail side-effect ledger                                      |
| 16        | `tests/message-authorization.test.ts`      |               8 | 1st-degree auth gate                                           |
| 17        | `tests/message-idempotency.test.ts`        |              22 | Duplicate message prevention                                   |
| 18        | `tests/null-vanity.test.ts`                |               8 | Null-vanity URL protection                                     |
| 19        | `tests/partial-update.test.ts`             |               4 | Dynamic SQL updates                                            |
| 20        | `tests/phase2-validation.test.ts`          |               8 | Phase 2 hardening checks                                       |
| 21        | `tests/phase3-guards.test.ts`              |               9 | Phase 3 hardening checks                                       |
| 22        | `tests/phase4-guards.test.ts`              |               5 | Phase 4 hardening checks                                       |
| 23        | `tests/phase5-guards.test.ts`              |               5 | Phase 5 hardening checks                                       |
| 24        | `tests/proxy-health-bypass.test.ts`        |               5 | Proxy routing                                                  |
| 25        | `tests/retry-ledger.test.ts`               |              20 | Ledger retry re-arm logic                                      |
| 26        | `tests/run-lifecycle-guards.test.ts`       |               5 | Run state machine transitions                                  |
| 27        | `tests/run-status-transition.test.ts`      |              10 | Status change permissions                                      |
| 28        | `tests/runner-claim.test.ts`               |              12 | Track claim concurrency (includes 4 parameterized loop states) |
| 29        | `tests/runner-connect-idempotence.test.ts` |              10 | Connect error idempotence (includes 4 error type tests)        |
| 30        | `tests/runner-loop-survival.test.ts`       |               6 | Loop exception resilience                                      |
| 31        | `tests/runner-visit-degree.test.ts`        |              13 | Visit degree enrichment                                        |
| 32        | `tests/runner-watchdog.test.ts`            |               9 | Watchdog threshold checks                                      |
| 33        | `tests/secret-migration.test.ts`           |               2 | Secret encryption upgrade                                      |
| 34        | `tests/session-read-path.test.ts`          |               3 | Storage state read security                                    |
| 35        | `tests/signup-closed.test.ts`              |               4 | Closed registration enforcement                                |
| 36        | `tests/slice-c-connection-state.test.ts`   |              33 | Connection status states (includes 3 headline tests)           |
| 37        | `tests/source-text-corpus.test.ts`         |               4 | Comment/string stripper corpus invariant across 166 files      |
| 38        | `tests/source-text-drift.test.ts`          |               3 | Source parser drift detection                                  |
| 39        | `tests/ssr-auth.test.ts`                   |               4 | SSR auth redirect verification                                 |
| 40        | `tests/step-identity.test.ts`              |              15 | Step UUID persistence                                          |
| 41        | `tests/support/source-text.test.ts`        |              15 | AST tokenizer helper tests                                     |
| 42        | `tests/todos-activity-logs.test.ts`        |               6 | Todo and activity logging                                      |
| 43        | `tests/visit-degree-observation.test.ts`   |              15 | Degree parsing accuracy                                        |
| 44        | `tests/visit-topcard.test.ts`              |              10 | Profile topcard scraping                                       |
| 45        | `tests/workflow-delete-guard.test.ts`      |               6 | Active run workflow delete refusal                             |
| 46        | `tests/workflow-duplicate.test.ts`         |               2 | Campaign duplication logic                                     |
| **Total** | **46 suites**                              | **432 passing** | **0 failed, 0 skipped, 0 cancelled**                           |

### Historical dependency triage — conclusions superseded (PR-10, 2026-09-17)

The original matrix below is retained solely as a historical record of the assertions being corrected, **not as current reachability evidence**. Its blanket `UNREACHABLE`, `NOT APPLICABLE`, and mitigation conclusions are withdrawn; the dated C1 correction section below governs the assessment.

Corrected counting: `npm audit --omit=dev` reported **20 vulnerable package entries (2 critical, 13 high, 5 moderate)**, not 20 distinct advisories across eight packages. The original eight-row matrix was incomplete and grouped several packages together.

| Package                                   | Severity            | Advisory IDs / Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Reachability & Exploitability in Linki                                                                                                                                                                                                                                                                                                                                                                                                               | Disposition / Action                                                             |
| ----------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| `next` (16.1.6)                           | Critical / High     | GHSA-p293-qw3h-jr36 (Windows RCE), GHSA-2xp9-vwfh-vxw4 (AVIF RCE in Image API), GHSA-955p-x3mx-jcvp (Server Functions), GHSA-m99w-x7hq-7vfj (Server Actions DoS), GHSA-89xv-2m56-2m9x (Server Actions SSRF), GHSA-4c39-4ccg-62r3 (Edge Server Actions), GHSA-36qx-fr4f-26g5 (i18n bypass), GHSA-492v-c6pp-mqqv (Middleware dynamic route bypass), GHSA-vfv6-92ff-j949 / GHSA-wfc6-r584-vfw7 (RSC cache), GHSA-c4j6-fc7j-m34r (WebSocket SSRF), GHSA-mg66-mrh9-m8jx / GHSA-h64f-5h5j-jqjh / GHSA-q8wf-6r8g-63ch (Image Optimization DoS) | **UNREACHABLE / NOT APPLICABLE in production deployment**: (1) Host is Linux container, Windows RCE cannot fire; (2) Linki uses Pages Router, not App Router (Server Actions, Server Functions, RSC are unused); (3) No i18n routing configured; (4) Auth enforced per-page/API, not Next Middleware; (5) No WebSockets configured; (6) Linki is single-tenant authenticated dashboard behind closed signup with no user-uploaded AVIF optimization. | Scheduled for upgrade to Next 16.3.5 in Phase C1 after build/smoke verification. |
| `next-auth` (4.24.13)                     | Critical / Moderate | GHSA-7rqj-j65f-68wh (Email provider homoglyph @ bypass), GHSA-xmf8-cvqr-rfgj (`getToken()` Bearer header exception), GHSA-x445-f3h2-j279 (OAuth state/nonce cookie binding)                                                                                                                                                                                                                                                                                                                                                             | **UNREACHABLE**: Linki uses `CredentialsProvider` only (single-owner password authentication with bcryptjs). No Email magic-link provider is configured; no OAuth providers are used; API endpoints use session cookies rather than bearer tokens.                                                                                                                                                                                                   | Patch/bump in C1 if non-breaking update available.                               |
| `nodemailer` (7.0.13)                     | High                | GHSA-c7w3-x93f-qmm8 / GHSA-vvjj-xcjg-gr5g (SMTP command injection in envelope/transport name), GHSA-268h-hp4c-crq3 (`List-*` header CRLF injection), GHSA-r7g4-qg5f-qqm2 (OAuth2 token TLS), GHSA-p6gq-j5cr-w38f / GHSA-wqvq-jvpq-h66f (`disableFileAccess` bypass), GHSA-2x7j-588g-ccc2 (O(n²) addressparser DoS)                                                                                                                                                                                                                      | **MITIGATED / UNREACHABLE**: SMTP settings are loaded from internal database settings, not arbitrary external input; Linki sends 1:1 outreach, does not set `List-*` headers; plain SMTP user/password is used, not OAuth2; email templates contain rendered HTML/text without file attachments; recipient address list is 1 per send.                                                                                                               | Scheduled for major version migration in C1 with PR-01 TLS hardening.            |
| `imap-simple` (5.1.0) / `utf7` / `semver` | High                | GHSA-c2qf-rxjj-qqgw (semver ReDoS via transitive dependency in utf7/imap)                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **UNREACHABLE**: Semver parsing in `utf7` is executed once at module load, not on received email contents or network input.                                                                                                                                                                                                                                                                                                                          | Safe bump in C1.                                                                 |
| `postcss` (<=8.5.22)                      | High                | GHSA-qx2v-qp2m-jg93, GHSA-6g55-p6wh-862q, GHSA-fxqj-rqcc-2cmp, GHSA-r28c-9q8g-f849 (XSS/arbitrary file read in sourcemaps)                                                                                                                                                                                                                                                                                                                                                                                                              | **BUILD-TIME ONLY**: PostCSS runs solely during `next build` inside the build container. Not exposed at runtime.                                                                                                                                                                                                                                                                                                                                     | Updated transitively with Next.js upgrade in C1.                                 |
| `qs` (2.2.5 - 6.15.3)                     | Moderate            | GHSA-x5fp-wj9c-mxmx (array-limit bypass), GHSA-4mjr-xmp4-gh2g (isBuffer DoS)                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **UNREACHABLE**: Linki uses Next.js standard URL query parser; `qs` is a transitive dependency not called on external query strings.                                                                                                                                                                                                                                                                                                                 | Non-breaking patch via `npm audit fix` in C1.                                    |
| `sharp` (<=0.35.4-rc.0)                   | High                | GHSA-f88m-g3jw-g9cj, GHSA-rgj7-g3m4-5g8c (libvips/libheif CVEs)                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **UNREACHABLE**: Sharp is an optional Next.js image optimization dependency; not invoked for profile scraping or UI rendering.                                                                                                                                                                                                                                                                                                                       | Updated transitively with Next.js upgrade in C1.                                 |
| `uuid` (<11.1.1)                          | Moderate            | GHSA-w5hq-g745-h8pq (Missing buffer bounds check in v3/v5/v6)                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **UNREACHABLE**: Transitive dependency in next-auth; next-auth uses `uuid.v4()` (random UUIDs) without passing custom buffers.                                                                                                                                                                                                                                                                                                                       | Updated transitively in C1.                                                      |

### Runtime versions (measured)

- Host CLI: Node v24.19.0, npm 11.17.0, Docker 29.1.3 (legacy builder).
- Verification image: Node **v22.23.0**, npm **10.9.8**, TS 5.9.3, Next 16.1.6, React 19.2.3, better-sqlite3 12.6.2 (native build verified via dlopen probe after noexec fix).
- Deployment configuration pins `node:22-slim@sha256:d9f8500...`, matching the verification base. This does not establish the deployed image, exact runtime, browser version, or image/browser advisory status; those remain unverified.

## C1 implementation evidence and corrections — 2026-09-17

**Status (superseded 2026-09-17): the final integrated gate has since passed and the PR-10 residual is resolved; see "C1 final integrated gate record" below. Reviewer sign-off remains pending.** This entry records the C1 implementation handoff as it stood before that gate, not a new deployment or a C1 completion decision. Focused-test counts and built HTTP smoke outcomes below are supplied by main's implementation evidence; this documentation-only pass did not rerun them. Dependency resolution and audits were observed in the PR-10 implementation session. No new image identity, integrated gate result, or reviewer signoff is inferred from focused results.

### Isolation and provenance

- Changes are working-tree changes on the recorded C0 base, not a newly committed/deployed revision. Main owns the final integrated snapshot and gate, currently running; its manifest hash, exact image ID, final counts and exit codes must be appended when available.
- Application checks use synthetic fixtures and disposable storage under the C0 isolation model, with mocked browser boundaries or loopback HTTP/mail services. No production changes, production mounts, production data access, or provider contact occurred in this work.
- PR-10 resolution used only copied `package.json`/`package-lock.json` in a disposable digest-pinned Node **22.23.0**, npm **10.9.8** container, clean environment, non-root user, read-only rootfs, dropped capabilities, and no lifecycle scripts. Network access was used for npm registry metadata/resolution/audit, not application providers. No `--force` or `--legacy-peer-deps` was used.
- C0 snapshots include current tracked contents and safe, nonignored untracked additions (`scripts/c0-verify.mjs:11`), including the new test files. Ignored local-only tests remain excluded. Reconcile the final snapshot and expanded runtime cases; do not substitute the historical 432 count or add overlapping focused counts together.

### Implemented controls and focused evidence

| Item | Implementation and evidence | Acceptance boundary |
| --- | --- | --- |
| PR-01: mail TLS | Strict certificate trust, expiry and configured-host identity verification across SMTP/IMAP; trusted synthetic CA succeeds, untrusted/expired/mismatched certificates fail. SMTP requires TLS: implicit TLS or mandatory STARTTLS, with unavailable/rejected STARTTLS failing before plaintext credentials or mail. Global TLS-disable configuration cannot override the mail policy. **31 mail tests pass**, covering `tests/email-tls-policy.test.ts`, `tests/email-tls.test.ts` and `tests/support/mail-tls-fixture.ts`. Source: `lib/email/tls.ts:6`, `lib/email/sender.ts:31`, and IMAP call sites. | Synthetic loopback evidence, not provider interoperability. Final integrated gate/reviewer pending. TLS hardening does not resolve every Nodemailer advisory. |
| PR-07: CSV/navigation | HTTPS LinkedIn Sales Navigator lead shape checked before CSV persistence, independently before enrichment navigation, and on legacy stored URLs used by profile scraping. Main-frame routing guards refuse disallowed intercepted navigations. **17 focused PR-07 tests pass** in `tests/pr07-csv-navigation.test.ts`. Source: `lib/linkedin-url.ts:72`, `lib/csv-import.ts:99`, `lib/linkedin/enrich.ts:51`, `lib/linkedin/profile-scrape.ts:134`. | Routing is not complete network confinement: Playwright route interception does not independently validate every HTTP redirect hop and may be bypassed by service-worker-handled requests. Main-frame guards do not promise a subresource firewall. No live provider/browser-session test or complete redirect/service-worker containment claim. |
| PR-13: auth/bootstrap | DB-backed `users.session_version` is checked against the JWT for API/SSR/session authorization. Password change increments the version, revoking all previously issued user sessions; legacy tokens lacking the version are invalidated. First-owner signup uses an atomic transaction for the ownership check and insert. **9 standalone PR-13 tests pass; 18 including associated suites** (overlapping selections, not 27). Coverage includes `tests/auth-pr13.test.ts`, `tests/auth-pr13-migration.test.ts`, signup and SSR suites. Source: `lib/auth.ts:5`, `lib/page-auth.ts:5`, `lib/db.ts:72`, `pages/api/auth/[...nextauth].ts`, `pages/api/auth/change-password.ts`, `pages/api/auth/signup.ts`. | User-session revocation, not rotation of the separate internal service secret. Final integrated gate/reviewer pending. |
| PR-13: built HTTP smoke | **PASS against an actual built Next 16.3.5 server**, using disposable users/storage and loopback HTTP: after revocation the old cookie receives **API 401**, **SSR 307**, and session response **`{}`**; fresh login succeeds. This exercises the built HTTP/auth path, not only mocks. | Focused smoke supplied by main, not a final whole-suite verdict or a production measurement. |

### Corrections to C0 advisory assertions

1. **Counts and coverage:** the earlier 20 total denotes vulnerable package entries, including inherited findings, not distinct GHSA advisories. The eight-row matrix omitted entries for `@hono/node-server`, `baseline-browser-mapping`, `brace-expansion`, `fast-uri`, `hono`, `imap`, `ip-address`, `linkify-it`, `mailparser`, and `nanoid`. All earlier blanket zero-exploitability statements are withdrawn.
2. **Framework conditions:** Linux and Pages Router can exclude specific advisory preconditions, not all Next vulnerabilities. `proxy.ts:36` does enforce API authentication, so the assertion that authentication does not rely on middleware/proxy was incorrect. No image imports or user-uploaded AVIF is not proof that the Image Optimization API/Sharp is inaccessible. `next.config.ts:10` does not disable that API; configuration and serving behavior need individual assessment. GHSA-mg66-mrh9-m8jx concerns Cache Components connection exhaustion, not Image Optimization DoS.
3. **Bearer path:** `getToken({ req })` in `lib/auth.ts:39` and `lib/page-auth.ts:7` can process request Authorization headers even when ordinary clients use cookies. The Bearer parsing path is reachable; current exception handling is mitigation, not absence of the path. The corresponding historical advisory is high severity. Credentials-only configuration narrows Email/OAuth-provider conditions but cannot justify a blanket NextAuth classification.
4. **Inbound mail:** outbound 1:1 recipients and database-held SMTP settings do not cover incoming RFC822 parsing. `lib/email/inbox.ts:89` passes received content to `simpleParser()` before output truncation. The old `mailparser@3.9.8` brought its own `nodemailer@8.0.5` and `linkify-it@5.0.0`; root Nodemailer was not the entire mail dependency surface. The C1 Mailparser upgrade addresses that separate dependency chain without declaring all mail processing safe.
5. **Transitives:** the old `utf7` constraint `~5.3.0` cannot admit patched semver 5.7.2 by an ordinary compatible refresh; C1 uses the explicit scoped override below. Next's PostCSS resolution and separate build-tool resolutions must all be reviewed, which requires both full and production audits. Next's query parser alone does not prove every transitive `qs` consumer unreachable. Retained UUID/semver/build-only reachability assertions are historical hypotheses, not blanket production guarantees.
6. **Images and gate limits:** npm audit covers the resolved npm graph, not Debian, Chromium, the deployed runtime or image vulnerabilities. Browser/image assessment remains unverified. C0's direct Next build invocation is not identical to `Dockerfile:58`, which uses `npm run build`. C0 currently records lint but excludes it from the mandatory verdict (`scripts/c0-check.mjs:95`); C1 acceptance must explicitly require lint success as well as tests/typecheck/build. A prior C0 exit 0 is not the pending final C1 gate result.

### PR-10 versions, resolution and final candidate audits

| Package / scope | Previous resolution | C1 resolution |
| --- | --- | --- |
| `next`, `eslint-config-next` | 16.1.6 | **16.3.5**, exact declarations kept aligned |
| `next-auth` | 4.24.13 | **4.24.15**, exact |
| `mailparser` | 3.9.8 | **3.9.28**, exact |
| Root `nodemailer` | 7.0.13 | **7.0.13** at handoff, explicitly pinned residual; **10.0.10** after the maintainer-approved final resolution below |
| Mailparser's nested `nodemailer` | 8.0.5 | **10.0.10** |
| `utf7`'s `semver` | 5.3.0 | **5.7.2**, scoped `overrides.utf7.semver`; not a global override |
| `uuid` | 8.3.2 | **11.1.1**, via NextAuth |
| `sharp` | 0.34.5 | **0.35.4**, via Next |
| `postcss` | 8.4.31 under Next; separate 8.5.8 | **8.5.23**, deduplicated compatible resolution satisfying Next's exact requirement |
| `qs`, `linkify-it` | 6.15.2; 5.0.0 | **6.16.0; 5.0.2** |

Targeted compatible transitive refreshes also covered `@hono/node-server`, `baseline-browser-mapping`, `brace-expansion`, `fast-uri`, `hono`, `ip-address`, `nanoid`, and dev-tool findings in `@babel/core`, `@humanfs/node`, `browserslist`, `flatted`, `js-yaml`, and `picomatch`. Existing application scripts, React **19.2.3**, TypeScript **5.9.3**, better-sqlite3 **12.6.2**, Playwright **1.58.2**, and the Node/Chromium deployment pins were not upgraded by PR-10.

Resolution used `npm install --package-lock-only --ignore-scripts --no-audit --no-fund --strict-peer-deps` and targeted `npm update ... --package-lock-only --ignore-scripts --no-audit --no-fund --strict-peer-deps`. The approved candidate resolved successfully; manifest/lock consistency, preservation of existing scripts, and `git diff --check` passed. These checks establish dependency resolution, not application correctness. Disposable manifest evidence remains at `/tmp/opencode/pr10-manifests-c8YL0j` for this session; final integrated evidence belongs to main's gate.

Final candidate audit observations on 2026-09-17 (Node 22.23.0 / npm 10.9.8):

| Command | Vulnerable package entries | Critical | High | Moderate | Low | Interpretation |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `npm audit --package-lock-only --ignore-scripts --json` | **2** | **0** | **2** | **0** | **0** | Full graph; findings present, not an audit-clean result |
| `npm audit --package-lock-only --ignore-scripts --omit=dev --json` | **2** | **0** | **2** | **0** | **0** | Production graph; same residual entries |

The two entries are root **`nodemailer@7.0.13`** and **`next-auth@4.24.15` inheriting Nodemailer's finding**. There are **10 underlying Nodemailer advisories**, not two distinct advisories or ten high-severity advisories. NextAuth's own previously listed direct advisories no longer appear in the candidate audit; its inherited entry remains. The ten IDs are GHSA-c7w3-x93f-qmm8, GHSA-vvjj-xcjg-gr5g, GHSA-268h-hp4c-crq3, GHSA-wqvq-jvpq-h66f, GHSA-r7g4-qg5f-qqm2, GHSA-p6gq-j5cr-w38f, GHSA-8m3c-c648-2xjj, GHSA-wmmp-3585-3rmp, GHSA-2x7j-588g-ccc2, and GHSA-cc9r-2j5m-2m83. They span SMTP/header injection, content-access policy bypasses, OAuth2 TLS, recipient-domain parsing and address-parser complexity; applicability must be assessed individually.

### Open residual, review ownership and C1 handoff (as recorded at handoff; resolved below)

Root Nodemailer **10.0.10** was attempted with strict peer resolution and rejected with **ERESOLVE**: `next-auth@4.24.15` declares optional peer `nodemailer@^7.0.7`. Optional does not permit an incompatible installed root peer. Root 7.0.13 is retained explicitly rather than using force/legacy flags, a dubious peer override, an automatic NextAuth downgrade, or an unapproved SMTP alias/source migration. Mailparser's separate nested Nodemailer 10.0.10 resolves normally. PR-01 TLS hardening does not close this dependency residual.

- **Proposal owner:** C1 implementation agent proposes a reviewed resolution, including any SMTP alias/source change only after main's approval.
- **Maintainer acceptance:** unassigned/pending; no reviewer signoff, risk acceptance, or dependency closure is invented here.
- **Next review:** before C1 signoff/deployment. Record the chosen peer-compatible migration or explicit residual decision, affected call paths, and acceptance evidence.
- **Final gate:** running under main. Append the actual integrated snapshot/image identity, test totals, lint/typecheck/build exits and reviewer decision when available. Focused counts above overlap and do not establish the final total. Retain the PR-07 redirect/service-worker limitation and unverified image/browser assessment in that decision.
- **Verdict:** C1 is not complete; PR-10 is not closed. No production rollout or production rollback action was performed.

## C1 final integrated gate record (appended 2026-09-17)

**Verdict: the final integrated gate passed on the complete C1 working tree, including the PR-10 resolution. C1 reviewer sign-off is pending and is not inferred here.**

| Field | Value |
| --- | --- |
| Timestamp | 2026-09-17, gate A checks completed 18:13Z; gate B (final) checks completed 18:19Z (evidence file mtimes) |
| Owner | Main agent (this session); maintainer present for the PR-10 decision |
| Source revision and intended diff | C0 base `ad1bf0d3fc356b49ae261b4371554aa249140208` plus the C1 working tree: 62 staged files (3098+/1069−) from the C1 implementation handoff, plus this session's 5-file change (`scripts/c0-check.mjs`, `scripts/c0-verify.mjs`, `package.json`, `package-lock.json`, this document). Not yet committed at the time of measurement. |
| Toolchain (measured in container) | Node **v22.23.0**, npm **10.9.8**, TypeScript 5.9.3, Next 16.3.5, React 19.2.3, better-sqlite3 12.6.2 (rebuilt from source in image), Nodemailer **10.0.10** |
| Image identity | Gate A (pre-PR-10-resolution manifests): `sha256:f0f0aae9003015ec2d53e2fde7618df8156c67ccae64f29cd126e80e29e0d0ed`, snapshot `1d00174c44228638a772aa01e0d4b6ea2f1a2152cf4d7b96dc2175b6b3b5c2dc`. Gate B (final, Nodemailer 10 lockfile): `sha256:9b3b98118689928931bd602e30503b502f2bf41636d0e645e2b76152f04f499a`, snapshot `84c382bac6ce7d80e4837d846c11e1ca5d0f684fa4d4fc8390ceff8d54dbf595`. Both snapshots: 227 files. Base `node:22-slim@sha256:d9f85009…` per `scripts/c0.Dockerfile:1`. |
| Isolation method | `npm run verify:c0`: tracked-only snapshot (tooling dirs excluded), `--network=none`, `--read-only`, non-root uid 1000, `--cap-drop=ALL`, `no-new-privileges`, clean environment with disposable secrets, tmpfs `/tmp` (noexec) and `/work` (exec). Isolation probe PASS in both runs. No production mounts, data, or provider contact. |
| Commands and exit codes (gate B) | `npm --version` 0 · native sqlite load 0 · `next typegen` 0 · `npm run typecheck` 0 · `npm test -- --test-reporter=tap` 0 · `npm run lint -- --max-warnings=0` 0 · `node scripts/mirror-ee.mjs` 0 · `node node_modules/next/dist/bin/next build` 0 · **gate exit 0**. Gate A: identical, all 0. |
| Counts | Tests: **490 pass / 0 fail / 0 skipped / 0 cancelled** in both gates (51 tracked suites: 46 from C0 plus `tests/auth-pr13.test.ts`, `tests/auth-pr13-migration.test.ts`, `tests/email-tls-policy.test.ts`, `tests/email-tls.test.ts`, `tests/pr07-csv-navigation.test.ts`). Lint: **0 errors / 0 warnings**, now a mandatory check. Typecheck and build: pass. |
| Focused reruns inside gate B image | Mail TLS (`tests/email-tls-policy.test.ts`, `tests/email-tls.test.ts`): **31 pass** against Nodemailer 10.0.10. PR-07 `tests/pr07-csv-navigation.test.ts`: **17 pass**. PR-13 standalone (`tests/auth-pr13.test.ts`, `tests/auth-pr13-migration.test.ts`): **10 pass** (correcting the handoff's "9"); with signup and SSR suites: **18 pass**, overlapping selections as before. |
| Sanitized evidence reference | Local evidence directories `/tmp/linki-c0-g4QWXZ` (gate A) and `/tmp/linki-c0-ldMlZr` (gate B): `manifest.json`, `image-id.txt`, `prepare.log`, `checks.log`. Retained images `linki-c0-3a4bc103-…` and `linki-c0-cf21558b-…`. Session-local; not committed. |
| Findings confirmed / retracted | Confirmed: the C1 implementation passes the whole-suite gate with lint mandatory. Corrected: the C0 snapshot script rejected tracked `.agents/` paths (the tracked TypeSafe skill), aborting gate run 1 before Docker; tracked tooling directories are now skipped, and the secret/database throw is unchanged. Corrected: PR-13 standalone count is 10, not 9. Retracted: the handoff's ERESOLVE conclusion for root Nodemailer 10 applied to an unscoped install; a scoped override resolves it under `--strict-peer-deps`. |
| Rollback readiness | No production change. Reverting is a working-tree/commit revert; the previous lockfile (Nodemailer 7.0.13) is the staged predecessor. No data migration was introduced by this session (PR-13's `users.session_version` migration belongs to the handoff and remains as recorded). |
| Unresolved blockers | (resolved 2026-09-18: sign-off recorded below; tree committed as `4835ffc` and pushed to `origin/main`.) Remaining: Image/browser (Debian, Chromium) advisory assessment remains unverified; `npm audit` scope is the npm graph only. PR-07 redirect/service-worker limitation stands. |
| Next action | Begin C2 (durable state, side-effect identity, import and worker ownership) — authorized by the sign-off below. |
| Reviewer decision | **Accepted 2026-09-18** — see "C1 sign-off" below. |

### PR-10 final resolution (maintainer-approved 2026-09-17)

Decision: upgrade root `nodemailer` to **10.0.10** and declare `overrides["next-auth"].nodemailer = "$nodemailer"`, scoped to NextAuth's optional peer edge; remove the now-redundant `@types/nodemailer` (Nodemailer 10 ships its own declarations and keeps the `@types/nodemailer` layout). Options considered in a disposable Node 22.23.0 / npm 10.9.8 container using copied manifests only:

| Option | Resolution (`--strict-peer-deps`) | Full / prod audit entries | Disposition |
| --- | --- | --- | --- |
| Keep 7.0.13 (handoff state) | n/a | 2 / 2 (root Nodemailer; NextAuth inherited) | Rejected by maintainer in favor of upgrade |
| Root 10.0.10 + scoped `next-auth` override | Resolves; single `nodemailer@10.0.10` deduped under root, `next-auth` (overridden) and `mailparser` | **0 / 0** | **Accepted** |
| Drop root Nodemailer, alias `npm:nodemailer@10` for SMTP | Resolves, but npm still installs `nodemailer@7.0.13` for NextAuth's optional peer | 2 / 2 | Rejected: no audit change, extra alias |

Why the override is scoped and safe here: `next-auth@4.24.15` references Nodemailer only in `providers/email.js` (verified in the packed tarball); this application configures only `CredentialsProvider` (`pages/api/auth/[...nextauth].ts:2`) and never imports the Email provider, so the overridden peer edge is not exercised at runtime. Nodemailer 7→10 breaking changes (Node ≥ 20; TLS verification for remote content fetches; `NoAuth` → `ENOAUTH`; SES v2 only) do not touch this codebase's call sites (`lib/email/sender.ts`: `createTransport`, `sendMail`, `verify`; no error-code matching; no attachments, OAuth2, proxies, or SES). Lockfile regenerated with `npm install --package-lock-only --ignore-scripts --no-audit --no-fund --strict-peer-deps`; `git diff --check` clean; no `--force` or `--legacy-peer-deps`.

Final audit on the committed candidate lockfile (Node 22.23.0 / npm 10.9.8, 2026-09-17): `npm audit --package-lock-only --ignore-scripts --json` → **0** entries; with `--omit=dev` → **0** entries. The ten Nodemailer 7 GHSA IDs listed above no longer apply to the resolved graph. This is an npm-graph statement only; Debian, Chromium and deployed-image assessment remain unverified.

### Gate script changes in this session

- `scripts/c0-check.mjs`: lint (`eslint --max-warnings=0`) is now part of the mandatory verdict, per C1 correction 6. Summary line renamed from `C0 LINT-BASELINE` to `C0 LINT`.
- `scripts/c0-verify.mjs`: tracked paths under `.agents/`, `.opencode/`, `.claude/` are skipped from the snapshot instead of aborting the gate; all other unsafe tracked paths (secrets, databases) still throw.

## C1 sign-off (2026-09-18)

| Field | Value |
| --- | --- |
| Decision | **C1 accepted.** PR-01, PR-07, PR-10 and PR-13 are closed at the acceptance boundaries stated in their register rows. |
| Reviewer | Maintainer (repository owner, `Viverun`), recorded in session by the main agent at the maintainer's instruction. |
| Basis | Final integrated gate B (exit 0; 490/490; lint 0/0; typecheck and build pass; Node 22.23.0 isolated; image `sha256:9b3b9811…`), host preflight PASS on Node 24.19.0 (516/516, eslint clean), `npm audit` 0/0, and the evidence record above. |
| Revision | `4835ffc` — C1 tree committed on `linkedin-connection-reliability`, fast-forwarded into `main`, pushed to `origin` (`Viverun/linki`) on 2026-09-18. `upstream` was not pushed. |
| Carried-forward limitations (not waived) | Deployed image / Debian / Chromium advisory assessment unverified; live mail-provider interoperability unverified; PR-07 route interception is not full redirect/service-worker confinement; internal service secret rotation out of scope. No production rollout is approved by this sign-off. |
| Authorization granted | **C2** (durable state, side-effect identity, import and worker ownership) may begin. C3/C4 remain unauthorized. |

## C2-A record (2026-09-18)

**Verdict: the isolated integrated gate passed on the complete C2-A working tree (PR-02, PR-04, PR-11, PR-12) on branch `c2a-durable-side-effects`. Reviewer sign-off is pending and is not inferred here.**

| Field | Value |
| --- | --- |
| Timestamp | 2026-09-18 (evidence directory `/tmp/linki-c0-SfC2BH`) |
| Owner | Main agent (this session) |
| Source revision | Branch `c2a-durable-side-effects`, HEAD `c95682f`; the six task commits: `57dc35a` (PR-02 SMTP classification), `8a5c609` (PR-04 fail-closed migrations + PR-02 ledger schema), `6eed149` (PR-02 email side-effect intent), `484b853` (PR-02 acceptance-judgment fix), `c50f288` (PR-12 retry step identity), `c95682f` (PR-11 atomic run creation) |
| Toolchain (measured in container) | Node **v22.23.0**, x64, digest-pinned base per `scripts/c0.Dockerfile:1` |
| Image identity | `sha256:63d201d2984f525d0b98e12c29b31811c84212de1fd9155472e2219757a92e82`; snapshot `4a17faca54720efa8f3196684978ba63bb1c95ba8f731cc7c5626f3655d954f1` (234 files; manifest revision `c95682f9b56ef74ecfb978a54da2f06bfcdc1ef0`) |
| Isolation method | `env PATH=/usr/bin:/bin /usr/bin/node scripts/c0-verify.mjs`: tracked-only snapshot, `--network=none`, non-root, clean environment. Isolation probe PASS: "non-root, loopback available, external network unreachable, clean environment". No production mounts. |
| Commands and exit codes | `npm-version` 0 · `native-sqlite` 0 · `typegen` 0 · `typecheck` 0 · `tests` 0 · `lint` 0 · `prebuild` 0 · `build` 0 · **gate: true** (`C0 SUMMARY`) |
| Counts (container) | Tests: **536 pass / 0 fail / 0 skipped** — 490 baseline plus the five new C2-A suites: `tests/smtp-failure-classification.test.ts` (20), `tests/migrations-fail-closed.test.ts` (5), `tests/email-idempotency.test.ts` (11), `tests/retry-step-identity.test.ts` (5), `tests/run-create-atomic.test.ts` (5). Matches the expected total exactly. Lint: clean (`C0 LINT: clean`). Typecheck and build: pass. |
| Counts (host preflight) | `bash scripts/preflight.sh` (nvm Node 24) → **PREFLIGHT PASS — safe to commit**; **562 pass / 0 fail / 0 skipped / 0 cancelled / 0 todo** (broader host suite selection than the container's tracked-only snapshot; eslint at or under baseline). |
| Evidence path | `/tmp/linki-c0-SfC2BH/` (`manifest.json`, `image-id.txt`, `checks.log`); retained image `linki-c0-623e31df-da39-4def-b048-8b3f4e0b7e49`; host preflight log `/tmp/preflight-test.log`. Session-local; not committed. |
| Findings confirmed / corrected | Confirmed: Task 1's plan called for command-only SMTP classification, but nodemailer reports every mid-transaction socket close as `ECONNECTION`/`CONN` — a message-text check for "Connection closed unexpectedly" was added so these stay classified ambiguous rather than misfiled as pre_send. Corrected: Task 3's first implementation judged send acceptance by comparing `accepted` to the raw target address; nodemailer normalizes accepted addresses (lowercase/trim), so a Critical review finding changed the acceptance judgment to `accepted.length > 0` for the single-recipient send (fixed in `484b853`, covered by test E11). email-idempotency carries **11** tests, not the 10 the brief anticipated. |
| Rollback readiness | All C2-A changes are code plus one backward-compatible ledger rebuild (`step_side_effects` widened to accept `action='email'` for legacy DBs, run inside the same IMMEDIATE transaction as the migration loop). Reverting the code leaves the widened CHECK constraint in place, which is harmless (a superset of allowed values, no data loss). No forward-only data migration was introduced. |
| Unresolved blockers | Legacy presence-guarded rebuild functions below the `migrations[]` loop still swallow errors (deliberate — they toggle `PRAGMA foreign_keys`, which a transaction would neutralise); open, C4 candidate. Three near-verbatim ledger blocks (message/InMail/email) in `lib/linkedin/runner.ts:` shared helper extraction deferred. Retry-blocked reasons interpolate step ids into operator-facing text. C2-B (PR-09, PR-06, PR-03) not started. |
| Next action | C2-B (PR-09, PR-06, PR-03) — not yet authorized; awaits C2-A reviewer sign-off. |
| Reviewer decision | **Accepted 2026-09-18** — see "C2 sign-off" below. |

## C2-B2 record (2026-09-18)

**Verdict: the isolated integrated gate passed on the complete C2-B2 working tree (PR-03: open-core reply hold and OOO policy) on branch `c2b2-reply-hold`. Reviewer sign-off is pending and is not inferred here.**

| Field | Value |
| --- | --- |
| Timestamp | 2026-09-18 (evidence directory `/tmp/linki-c0-N1XiIZ`) |
| Owner | Main agent (this session) |
| Source revision | Branch `c2b2-reply-hold`, HEAD `977925a`; the eight task commits: `d994e74` (Task 1 — undecided email replies hold both tracks before any send; `open_core_attempts` column), `4e3bb80` (Task 2 — open-core reply policy: Jev OOO judgment, explicit threshold, fail-closed retries; `@typesafe-ai/sdk@0.6.0`), `c9f93c5` (Task 2 fix — in-flight operator decisions respected; transactional fail-closed path; bounded return-date horizon), `f6d6435` (Task 3 — inbox sync routes captured replies to premium or the open-core policy, retries undecided replies), `f01b82c` (Task 4 — `POST /api/inbox/:replyId/resume-followup`), `218ee7a` (Task 5 — premium-status capabilities; reply-policy threshold settings route), `df922b9` (Task 6 — inbox resume UI + open-core decision line; TypeSafe integration and OOO threshold settings; visible LinkedIn limit), `977925a` (Task 6 fix — run-start open-core notice log made best-effort) |
| Toolchain (measured in container) | Node **v22.23.0**, x64, digest-pinned base per `scripts/c0.Dockerfile:1`. `@typesafe-ai/sdk@0.6.0` resolved at root with `--strict-peer-deps` in the Node 22.23.0 image, no nested deps; full `npm audit` `{"info":0,"low":0,"moderate":0,"high":0,"critical":0,"total":0}`; `npm audit --omit=dev` `{"info":0,"low":0,"moderate":0,"high":0,"critical":0,"total":0}` (Task 2 report). |
| Image identity | `sha256:2bf1c2c391ec57ecd6e77a0f4c65d7c356d2c0e7b3e901a7dfe744f171ae5aa3`; snapshot `f1b16e4eff9ced00885ef137cbe9e23843f76309591348c54cf2d41cfb42ba1c` (246 files; manifest revision `977925a38c91b17b4dd6ab67c26dc8bd7a6a95d6`) |
| Isolation method | `env PATH=/usr/bin:/bin /usr/bin/node scripts/c0-verify.mjs`: tracked-only snapshot, `--network=none`, non-root, clean environment. Isolation probe PASS: "non-root, loopback available, external network unreachable, clean environment". No production mounts. |
| Commands and exit codes | `npm-version` 0 · `native-sqlite` 0 · `typegen` 0 · `typecheck` 0 · `tests` 0 · `lint` 0 · `prebuild` 0 · `build` 0 · **gate: true** (`C0 SUMMARY`) |
| Counts (container) | Tests: **563 pass / 0 fail / 0 skipped** — 536 C2-A baseline plus the five new C2-B2 suites: `tests/reply-hold.test.ts` (4), `tests/reply-policy.test.ts` (15), `tests/inbox-sync-opencore-dispatch.test.ts` (2), `tests/inbox-resume-followup.test.ts` (4), `tests/premium-status-capabilities.test.ts` (2). Matches 536 + 4 + 15 + 2 + 4 + 2 = 563 exactly; `reply-policy.test.ts` carries **15** tests (P1–P12, D1–D3), not the 12 the brief's Task 7 estimate anticipated, because Task 2's fix round added P11, P12 and D3 (see Findings below). Lint: clean (`C0 LINT: clean`). Typecheck and build: pass. |
| Counts (host preflight) | `bash scripts/preflight.sh` → **PREFLIGHT PASS — safe to commit**; **589 pass / 0 fail** (`grep -E "^ℹ (pass|fail)" /tmp/preflight-test.log`). Higher than the container's 563 because the host suite selection also runs git-ignored local-only suites — expected, not a discrepancy (consistent with the C2-A record's 562-vs-536 gap). |
| Evidence path | `/tmp/linki-c0-N1XiIZ/` (`manifest.json`, `image-id.txt`, `checks.log`); retained image `linki-c0-41f08022-2d68-4b62-b641-b425dc43b588`; host preflight log `/tmp/preflight-test.log`. Session-local; not committed. |
| Findings confirmed / corrected | Critical (Task 2 review): decision writes applied the `email_replied_at` stamp/log even when the guarded `dispatched_at IS NULL` UPDATE lost to an in-flight operator decision — fixed in `c9f93c5` with new test P11, which now asserts the operator's decision wins and no stamp is applied. Also fixed in `c9f93c5`: the fail-closed path (three judgment failures) was not transactional — now wrapped in the same `db.transaction(...).immediate()` pattern as the other decision paths. Controller ruling R3 refined the return-date handling beyond the spec's literal text: year-less dates that resolve to a this-year occurrence ≤ 60 days in the past are treated as stale (no reschedule, covered by new test D3), and any resolved return date more than 180 days out is treated as none rather than pushing a far-future `next_step_at` (covered by new test P12). Task 6 (`977925a`): the run-start open-core notice log write is best-effort — a logging failure there must not block or fail the run start. |
| Rollback readiness | The `open_core_attempts` column added on `email_replies` is additive (no destructive schema change; safe with or without the policy code). Removing the TypeSafe integration key disables judgments: `defaultJudge` then throws before any network call, so `decideReplyOpenCore` treats every reply as a judge failure. Corrected (fix wave, see C2-B2 fix-wave record below): replies are **not** held indefinitely — each undecided reply is held, retried by the open-core sweep, and fails closed to `human_reply` after three sync cycles (`MAX_ATTEMPTS`) without a decision, which stops follow-ups the normal way. An operator's resume-followup (or cancel-followup) call undoes either state — a held row or an already-fail-closed one — restarting judgment or re-arming the track as appropriate. This is a safe, fail-closed degradation, not a silent bypass or an unbounded stall. Reverting the `c2b2-reply-hold` branch entirely restores pre-hold behaviour: follow-ups continue after a reply is received, i.e. the original PR-03 defect this phase fixes. The fix wave adds one forward-only data migration (`backfillLegacyUndecidedReplies`, see below) — additive, marker-gated, and it only ever sets `dispatched_at`/`dispatch_result_json` on rows that were already NULL. |
| Residuals / unresolved | Judgment quality on real mail is **Unverified** — all evidence is from synthetic samples; `scripts/typesafe-smoke.mjs` is the only manual/live check available. LinkedIn reply detection remains premium-gated (open-core email-only). `tests/reply-policy.test.ts` P7 does not stub `fetch` (network-call behavior on a missing key is inferred from the code path, not observed over the wire). Unparseable weekday date candidates are still offered to the SDK's `choice` call rather than filtered beforehand. Premium coexistence assumption (ruling R6): the `ee/` dispatcher is assumed to set `dispatched_at` synchronously for every bucket including OOO, so that the runner's undecided-reply hold (`lib/linkedin/runner.ts` ~1032) never fires under premium — **Unverified** here (`ee/` is not in this repo); confirm before any premium deployment. C2-B1 (PR-09, PR-06) not started. |
| Next action | C2-B1 (PR-09, PR-06) — not yet authorized; awaits C2-A and C2-B2 reviewer sign-off. |
| Reviewer decision | **Accepted 2026-09-18** — see "C2 sign-off" below. |

### C2-B2 final-review fix wave (2026-09-18)

Applied on branch `c2b2-reply-hold` after the record above, in one commit ("C2-B2: final-review fixes — undecided holds have a UI exit; sweep independent of IMAP and bounded; operator cancel is a decision; legacy reply backfill; resume keeps audit trail; test gaps"): (1) `pages/inbox.tsx` — an undecided reply (`reply_id` set, `dispatched_at` NULL) now always shows "Resume follow-ups" and an "Awaiting decision" / "Awaiting decision — retrying" badge, so an operator is never stuck without an exit; "Pending" filter relabelled "Awaiting decision". (2) `lib/email/inbox.ts` — the open-core sweep (`runOpenCoreSweep`, now exported) runs in a `finally` after the IMAP step regardless of success/failure/throw, and `retryUndecidedReplies` is bounded to 10 rows per call (was 50) via a new options parameter, so one sweep cannot monopolise a runner/sync tick. (3) `pages/api/inbox/[replyId]/cancel-followup.ts` — cancelling now stamps `dispatched_at`, so a cancelled-but-previously-undecided reply is a decided row and the sweep cannot re-judge and overwrite it. (4) **Legacy reply backfill** — `lib/db.ts` `backfillLegacyUndecidedReplies`, run once inside the same migration transaction as `widenStepSideEffectActions`, gated by the `app_settings` marker `c2b2_reply_backfill_done`: replies captured before this phase existed (and were therefore never decided and never held) are marked decided (`dispatched_at = created_at`, a `"source":"migration"` JSON payload) so the new hold behaviour does not retroactively freeze months-old campaigns on replies nobody ever intended to hold. Covered by `tests/reply-backfill-migration.test.ts`. (5) `pages/api/inbox/[replyId]/resume-followup.ts` — resuming now merges onto the prior `dispatch_result_json` (keeping `p_ooo`, `reason`, etc.) instead of replacing it, and the re-arm predicate also matches tracks skipped with `'Follow-up cancelled from inbox'`, so resume-after-cancel works. (6) `lib/email/reply-policy.ts` — `our_last_email.body`/`.subject` are now truncated (`BODY_LIMIT`/500 chars) before being sent to the judge, matching the reply body's existing truncation. (7) New/updated tests: `tests/reply-hold.test.ts` H5 (LinkedIn track hold, no session opened) and H6 (`last_replied_at` skip); `tests/inbox-sync-opencore-dispatch.test.ts` S3–S5 exercise the new `runOpenCoreSweep` helper directly (IMAP's own connection lifecycle was judged infeasible to mock realistically here, per the brief's fallback); `tests/inbox-cancel-followup-decides.test.ts` and `tests/reply-backfill-migration.test.ts` are new; `tests/inbox-resume-followup.test.ts` gained R5 and an audit-trail assertion in R2.

## C2-B1 record (2026-09-19)

**Verdict: the isolated integrated gate passed on the complete C2-B1 working tree (PR-09: worker/browser ownership; PR-06: import checkpoints and recovery) on branch `c2b1-ownership`. Reviewer sign-off is pending and is not inferred here.**

| Field | Value |
| --- | --- |
| Timestamp | 2026-09-19 (evidence directory `/tmp/linki-c0-KpFA6l`) |
| Owner | Main agent (this session) |
| Source revision | Branch `c2b1-ownership`, HEAD `9453002`; eleven commits over `main`: `0d8182b`, `ce63d12`, `61602c0`, `1956dbd` (Tasks 1–2 — runner lease and standby), `02568cb` (Task 3 fix — browser owner claims the slot synchronously, before any `await`), `dc4610c` (Task 3 — `getSessionPage` and every context bypass migrated through the browser owner; busy browser reschedules the step; routes answer 409), `0d12cbb` (Task 5 — scraper reports each verified page; a missing page ends the window without advancing), `384e04d` (Task 6 — atomic import claim, per-page durable checkpoints under an owner fence, stall/cancel truthfulness, recovery and quarantine), `e4eb9df` (Task 6 fix — terminal import writes fenced with a per-claim token; recovery finishes exhausted windows and honours cancellation; `importedToday` counts durable inserts), `a142d1d` (gate-only fix — ref'd the teardown-gap timer; added O11), `9453002` (gate-only fix — hoisted mid-file top-level imports above the first `test()` in four test files, so Node 22 does not run `after()` before late-registered tests), `14c0e76` (this record; spec/plan corrected to the TTL ruling and the superseded owner code), `6966ec7` (final-review fix wave — shutdown drains browser holds before releasing the lease; import checkpoints require the lease; B2 eviction restored; fenced claim; early-return fences; log wording; docs). The gate fields in this table were measured at `9453002`; the fix wave was re-gated separately — see the subsection below |
| Toolchain (measured in container) | Node **v22.23.0**, x64, digest-pinned base per `scripts/c0.Dockerfile:1`. |
| Image identity | `sha256:3c1da9c219f8a9be8e35b22b7b58d2de527dc7e52f817db7c6681285059c4797`; snapshot `6fd7ba774f923c38cdbfbd668d36dd13d36b3438a96e8acc9ac5bc314ca75708` (260 files; manifest revision `9453002089677ceef13077430260e71dfa7ca568`) |
| Isolation method | `env PATH=/usr/bin:/bin /usr/bin/node scripts/c0-verify.mjs`: tracked-only snapshot, `--network=none`, non-root, clean environment. Isolation probe PASS: "non-root, loopback available, external network unreachable, clean environment". No production mounts. |
| Commands and exit codes | `npm-version` 0 · `native-sqlite` 0 · `typegen` 0 · `typecheck` 0 · `tests` 0 · `lint` 0 · `prebuild` 0 · `build` 0 · **gate: true** (`C0 SUMMARY`) |
| Counts (container) | Tests: **616 pass / 0 fail / 0 cancelled / 0 skipped**. Reconciles exactly against the prior BLOCKED attempt's 615 total plus the new O11 test added alongside the a142d1d timer fix: 615 + 1 = 616. Lint: clean (`C0 LINT: clean`). Typecheck and build: pass. |
| Counts (host preflight) | `bash scripts/preflight.sh` → **PREFLIGHT PASS — safe to commit**; **642 pass / 0 fail** (`grep -E "^ℹ (pass|fail)" /tmp/preflight-test.log`). Higher than the container's 616 because the host suite selection also runs git-ignored local-only suites — expected, not a discrepancy (consistent with the C2-A and C2-B2 records' host-vs-container gaps). |
| Evidence path | `/tmp/linki-c0-KpFA6l/` (`manifest.json`, `image-id.txt`, `checks.log`, `prepare.log`); retained image `linki-c0-fe8e5f83-103f-4115-b3b2-9df310308bd6`; host preflight log `/tmp/preflight-test.log`. Session-local; not committed. |
| Findings confirmed / corrected | (1) Lease TTL: the plan's `LEASE_TTL_MS = 120_000` was too short for a single step that can run ~345 s; corrected to `600_000`, renewed on every fenced track write and before each step (ruling R4). (2) Critical (review): the plan's reference browser-owner code claimed the slot after an `await`, admitting two same-tick acquirers; fixed in `02568cb` with the slot claimed synchronously before any `await` (tests O8, O9). (3) Critical (review): terminal import writes were unfenced; fixed in `e4eb9df` with a per-claim owner token (test I7). (4) Rulings deviating from the spec's literal text: `importedToday` counts running rows regardless of date plus terminal rows by `finished_at` (R12); recovery finishes an exhausted window as `done` (R11) and honours `cancel_requested`. (5) Two gate-only failures surfaced only under the isolated container's Node 22.23.0, not on the host's Node 24: an unref'd teardown-gap timer kept the event loop alive past the test runner's own forced end, draining it mid-wait (fixed `a142d1d`, ref'd the timer, added test O11); mid-file top-level imports in four test files let Node 22's runner execute `after()` before those files' later-registered tests ran, closing the shared DB out from under them (fixed `9453002`, imports hoisted above the first `test()`). Lesson recorded: verification runtime is not the host runtime — a gate that only ever ran on the host would not have caught either bug. (6) Final review: graceful shutdown released the lease before stopping in-flight browser holds (Critical) — fixed: shutdown aborts all local browser owners and waits for them to settle before releasing the lease; the per-page import checkpoint now also requires the runner lease. |
| Rollback readiness | The four `list_imports` columns this phase adds are additive (no destructive schema change; safe with or without the ownership/checkpoint code). The `app_settings.runner_lease` row is harmless if the code is reverted — an orphaned row with no reader. |
| Residuals / unresolved | Verified with fake browser objects and one process — not with two real Chromium stacks, so cross-process contention is not observed end-to-end. Synthetic imports only — not live Sales Navigator. `last_step_at`/`pending_reply_context` touches remain unfenced. The stall-first-page branch is unreachable with the real scraper (commented, kept). Import max hold is 3 h against a worst-case run of roughly 3.5 h. `authenticateAccount` and headless login open their own browser outside the ownership guard (pre-existing, not introduced by this phase). Concurrent SIGTERM+SIGINT run two best-effort shutdown sequences (idempotent; whichever finishes first exits). Reviewed: per-task reviews, one whole-branch review (sonnet — the opus dispatch died on a rate limit), one scoped re-review of the fix wave (Approved); maintainer sign-off pending. |
| Next action | Signed off 2026-09-19 (see the C2-B1 sign-off section); C3 remains unauthorized. |
| Reviewer decision | **Accepted** — maintainer, 2026-09-19 (recorded below). |

### C2-B1 final-review fix wave and final gate (2026-09-19)

Applied on branch `c2b1-ownership` after the record above, in one commit `6966ec7` ("C2-B1: final-review fixes — shutdown drains browser holds before releasing the lease; import checkpoints require the lease; B2 eviction restored; fenced claim; early-return fences; log wording; docs"): (1) **Critical** — `lib/linkedin/ownership.ts` gains `abortAllBrowserOwners(reason, settleTimeoutMs = 30_000)` (aborts every held slot's signal, closes its pages, waits — bounded — for each holder's `release()` to run); `lib/linkedin/runner.ts` SIGTERM/SIGINT handling is now abort-all-holds → release lease → log → `process.exit(0)`, so a rolling restart cannot leave this process's Chromium driving an account a replacement has just taken over (test O12). (2) `lib/import-jobs.ts` — the per-page checkpoint runs inside `withLease` (IMMEDIATE, fenced on the runner lease) so a process without the lease cannot durably checkpoint an import; `LeaseLostError` is handled like `ImportOwnershipLostError` (test I10 proves no partial insert and an untouched row). (3) `lib/linkedin/session.ts` — B2 retry evicts the closed context from the map explicitly. (4) `claimScheduledImport` runs under `withLease`. (5) The `LeaseLostError` step log no longer claims "no state written". (6) The list-deleted / `maxPages < 1` early-return updates are owner-fenced; the unreachable stall-first-page branch is commented. (7) `docs/operations.md` records the drain. Scoped re-review of `14c0e76..6966ec7` (sonnet): **Approved**, all seven items verified, no Critical/Important findings.

| Field | Value |
| --- | --- |
| Source revision | `6966ec7` (manifest revision `6966ec7487792bbeb07bb77b5c6f8329328380eb`); this docs commit follows it and changes no code. |
| Isolation | `env PATH=/usr/bin:/bin /usr/bin/node scripts/c0-verify.mjs`; isolation probe PASS ("non-root, loopback available, external network unreachable, clean environment"); no production mounts. |
| Image / snapshot | `sha256:b198befb6e78b78c0829043c0d47a8127997815a827dd619f231df2c5fab872e`; snapshot `479bb6cda7866dbe3c71cc12187c202600fc496f9b48b5243425737e2b4622b5` (260 files); retained image `linki-c0-572d2480-e285-4510-bd0d-5cee1e506cbc`; evidence `/tmp/linki-c0-X0F1Tv` (session-local). |
| Commands and exit codes | `npm-version` 0 · `native-sqlite` 0 · `typegen` 0 · `typecheck` 0 · `tests` 0 · `lint` 0 · `prebuild` 0 · `build` 0 · **gate: true** |
| Counts (container, Node v22.23.0) | **618 pass / 0 fail / 0 cancelled / 0 skipped** — reconciles as 616 (record above) + O12 + I10. Lint clean. |
| Counts (host) | Fix-wave suites 39/39 on the host and 39/39 inside the retained gate image before commit; pre-commit preflight passed on `6966ec7`. |

## C2 sign-off — C2-A and C2-B2 (2026-09-18)

| Field | Value |
| --- | --- |
| Decision | **C2-A accepted** (PR-02, PR-04, PR-11, PR-12 closed at their register boundaries). **C2-B2 accepted** (PR-03 closed at its register boundary: open-core out-of-office detection is a calibrated Jev judgment behind an explicit threshold, validated on synthetic samples only; LinkedIn reply detection remains premium). |
| Reviewer | Maintainer (repository owner, `Viverun`), recorded in session by the main agent at the maintainer's instruction. |
| Basis | C2-A: isolated gate exit 0, 536/536, lint 0/0, image `sha256:63d201d2984f525d0b98e12c29b31811c84212de1fd9155472e2219757a92e82`; host preflight 562/562; merged-tree host run 562/562. C2-B2: isolated gate exit 0, 563/563, lint 0/0, image `sha256:2bf1c2c391ec57ecd6e77a0f4c65d7c356d2c0e7b3e901a7dfe744f171ae5aa3`; host preflight 589/589; merged-tree host run 596/596; `@typesafe-ai/sdk` 0.6.0 audits 0/0 (full and production). Per-task and whole-branch reviews recorded in the respective records; one Critical per phase was found by review and fixed before merge (C2-A: acceptance judged by address equality; C2-B2: decision writes negating an in-flight operator decision). |
| Revisions | C2-A `12f3953` (fast-forwarded to `main` 2026-09-18); C2-B2 `40e99be` (fast-forwarded to `main` 2026-09-18); C2-B2 residual R7 fixed in `951c4ec`. All pushed to `origin` (`Viverun/linki`); `upstream` not pushed. |
| Carried-forward limitations (not waived) | Judgment quality on real mail **Unverified** (`scripts/typesafe-smoke.mjs` is the manual check); premium coexistence assumption R6 **Unverified** (the `ee/` dispatcher is assumed to set `dispatched_at` synchronously for every bucket — confirm before any premium deployment); legacy presence-guarded rebuild functions in `lib/db.ts` still swallow errors; three near-verbatim ledger blocks in `lib/linkedin/runner.ts`; deployed image / browser advisory assessment unverified. No production rollout is approved by this sign-off. |
| Authorization granted | **C2-B1** (PR-09 worker/browser ownership, PR-06 import checkpoints and recovery) may begin. C3/C4 remain unauthorized. |

## C2 sign-off — C2-B1 (2026-09-19)

| Field | Value |
| --- | --- |
| Decision | **C2-B1 accepted** (PR-09 worker/browser ownership and PR-06 import checkpoints/recovery closed at their register boundaries). With C2-A and C2-B2 accepted 2026-09-18, **phase C2 is complete.** |
| Reviewer | Maintainer (repository owner, `Viverun`), recorded in session by the main agent at the maintainer's instruction ("sign off and push"). |
| Basis | Isolated gate on the final code tree `6966ec7`: exit 0, 618/618, lint 0/0, Node v22.23.0, image `sha256:b198befb6e78b78c0829043c0d47a8127997815a827dd619f231df2c5fab872e`, snapshot `479bb6cd…`; merged-tree host run on `main` 644/644; pre-commit preflight passed on every commit. Reviews: per-task reviews (Criticals found and fixed before merge: non-atomic browser-owner claim `02568cb`, unfenced terminal import writes `e4eb9df`), one whole-branch review (Critical found and fixed: lease released before in-flight browser holds stopped, `6966ec7`), one scoped re-review of the fix wave (Approved). Two gate-only Node 22 failures were found by the isolated gate and fixed (`a142d1d`, `9453002`). |
| Revisions | Branch `c2b1-ownership` (14 commits over `c545ccc`, head `3b5f10e`) fast-forwarded to `main` 2026-09-19 and pushed to `origin` (`Viverun/linki`); `upstream` not pushed; branch deleted. |
| Carried-forward limitations (not waived) | Cross-process contention verified with fake browser objects in one process — two real Chromium stacks **Unverified**; imports verified against synthetic fixtures — live Sales Navigator **Unverified**; `last_step_at`/`pending_reply_context` touches unfenced; import max hold 3 h against a worst-case ~3.5 h window (abort → error branch, recovered from page+1 by the next holder); `authenticateAccount` and headless login open a browser outside the ownership guard (pre-existing); concurrent SIGTERM+SIGINT run two idempotent best-effort shutdown sequences. Plus the C2-A/C2-B2 limitations carried above. No production rollout is approved by this sign-off. |
| Authorization granted | None beyond this record. **C3 and C4 remain unauthorized**; a separate maintainer instruction is required to begin C3. |
